// Additive revision-cloud families v17.
// v16 remains unchanged. v17 re-runs the exploded q/cm detector on every page,
// then deduplicates by geometry instead of blocking a whole page when older families found something.
import { detectAdditiveRevisionCloudFamilies as detectV16, removeAdditiveRevisionCloudFamilies as removeV16 } from './revision-cloud-additive-families-v16.js?v=20260831-qcm1';

const SRC='vector-red-exploded-qcm-cloud-v17';
const NUM='[-+]?(?:\\d+(?:\\.\\d*)?|\\.\\d+)';
const RED=new RegExp(`(?:^|\\s)(1(?:\\.0+)?\\s+0(?:\\.0+)?\\s+0(?:\\.0+)?\\s+RG)`,'g');
const FRAG=new RegExp(`^\\s*q\\s+(${NUM})\\s+0\\s+0\\s+\\1\\s+0\\s+0\\s+cm\\s+q\\s+1\\s+0\\s+0\\s+1\\s+(${NUM})\\s+(${NUM})\\s+cm\\s+0\\s+0\\s+m\\s+([\\s\\S]*?)\\s+S\\s+Q\\s+Q`);
const OP=new RegExp(`((?:${NUM}\\s+){2,6})(c|l)\\s*`,'g');
const toText=b=>{const u=b?.asUint8Array?b.asUint8Array():b;let s='';for(let i=0;i<u.length;i+=0x8000)s+=String.fromCharCode(...u.subarray(i,Math.min(u.length,i+0x8000)));return s};
const toBytes=s=>{const b=new Uint8Array(s.length);for(let i=0;i<s.length;i++)b[i]=s.charCodeAt(i)&255;return b};
const resolve=o=>{try{return o?.resolve?.()||o}catch(_){return o}};
const streamRef=o=>{try{if(o?.isStream?.())return o;const r=resolve(o);return r?.isStream?.()?r:null}catch(_){return null}};
function refs(page){try{const c=page.getObject()?.get?.('Contents');if(!c)return[];if(c?.isArray?.())return Array.from({length:Number(c.length||0)},(_,i)=>streamRef(c.get(i))).filter(Boolean);const s=streamRef(c);return s?[s]:[]}catch(_){return[]}}
const add=(b,x,y)=>b?[Math.min(b[0],x),Math.min(b[1],y),Math.max(b[2],x),Math.max(b[3],y)]:[x,y,x,y];
const dist=(a,b)=>Math.hypot(a[0]-b[0],a[1]-b[1]);
function median(a){if(!a.length)return 0;const b=[...a].sort((x,y)=>x-y),m=Math.floor(b.length/2);return b.length%2?b[m]:(b[m-1]+b[m])/2}
function readBlock(text,redEnd){let pos=redEnd,bbox=null,paths=0,curves=0,lines=0,connected=0,prevEnd=null,diags=[];while(pos<text.length){const m=FRAG.exec(text.slice(pos));if(!m)break;const scale=Number(m[1]),tx=Number(m[2]),ty=Number(m[3]),body=m[4];if(!(scale>0&&scale<10))break;let cur=[tx*scale,ty*scale],pb=add(null,cur[0],cur[1]);bbox=add(bbox,cur[0],cur[1]);if(prevEnd&&dist(prevEnd,cur)<=Math.max(3,scale*8))connected++;let lc=0,ll=0,om;OP.lastIndex=0;while((om=OP.exec(body))){const ns=om[1].trim().split(/\s+/).map(Number);if(om[2]==='c'&&ns.length!==6)continue;if(om[2]==='l'&&ns.length!==2)continue;for(let i=0;i<ns.length;i+=2){const x=(tx+ns[i])*scale,y=(ty+ns[i+1])*scale;bbox=add(bbox,x,y);pb=add(pb,x,y);cur=[x,y]}if(om[2]==='c'){curves++;lc++}else{lines++;ll++}}if(lc<1||lc>10||ll>1)break;paths++;prevEnd=cur;diags.push(Math.hypot(pb[2]-pb[0],pb[3]-pb[1]));pos+=m[0].length}if(paths<50||curves<100||lines>Math.max(5,Math.floor(paths*.05))||!bbox)return null;const w=bbox[2]-bbox[0],h=bbox[3]-bbox[1],diag=Math.hypot(w,h);if(Math.min(w,h)<100||Math.max(w,h)>25000||Math.max(w,h)/Math.max(1,Math.min(w,h))>30)return null;const conn=paths>1?connected/(paths-1):0;if(conn<.85||median(diags)/Math.max(1,diag)>.12)return null;return{end:pos,pathCount:paths,curveCount:curves,lineCount:lines,bbox,connectedFraction:conn}}
function scanStream(text,refIndex){const out=[];RED.lastIndex=0;let m;while((m=RED.exec(text))){const start=m.index+(m[0].length-m[1].length),p=readBlock(text,RED.lastIndex);if(p)out.push({source:SRC,refIndex,start,...p})}return out}
function scanPage(page){const streams=[],candidates=[];for(const ref of refs(page)){let text;try{text=toText(ref.readStream())}catch(_){continue}const refIndex=streams.length;streams.push({ref,text});candidates.push(...scanStream(text,refIndex))}return{streams,candidates}}
const pub=c=>({bbox:c.bbox,source:SRC,exactRGB:[1,0,0],vectorAdditiveFamilyProof:true,vectorAdditiveFamilyMode:'red-exploded-qcm-curved-block-v17',vectorAdditiveFamilyPathCount:c.pathCount,vectorAdditiveFamilyCurveCount:c.curveCount,vectorAdditiveFamilyLineCount:c.lineCount,vectorAdditiveFamilyConnectedFraction:c.connectedFraction});
const close=(a,b,t=3)=>Array.isArray(a)&&Array.isArray(b)&&a.length>=4&&b.length>=4&&a.slice(0,4).every((v,i)=>Math.abs(Number(v)-Number(b[i]))<=t);
function mergeDedup(a,b){const map=new Map();for(const p of [...(a||[]),...(b||[])]){const n=Number(p?.page||0);if(!n)continue;if(!map.has(n))map.set(n,[]);const arr=map.get(n);for(const c of p.clouds||[]){if(!arr.some(x=>close(x?.bbox,c?.bbox,3)))arr.push(c)}}return[...map].map(([page,clouds])=>({page,clouds})).sort((x,y)=>x.page-y.page)}
async function detectNew(data){const mupdf=await import('https://cdn.jsdelivr.net/npm/mupdf@1.28.0/dist/mupdf.js'),doc=mupdf.PDFDocument.openDocument(new Uint8Array(data),'application/pdf'),out=[];try{for(let pi=0;pi<doc.countPages();pi++){const s=scanPage(doc.loadPage(pi));if(s.candidates.length===1)out.push({page:pi+1,clouds:[pub(s.candidates[0])]})}}finally{doc.destroy()}return out}
async function removeNew(data,pages){const wanted=[];for(const p of pages||[])for(const c of p?.clouds||[])if(c?.source===SRC&&c?.vectorAdditiveFamilyProof===true)wanted.push({page:Number(p.page||0),cloud:c});if(!wanted.length)return{data:new Uint8Array(data),removed:0,details:[]};const mupdf=await import('https://cdn.jsdelivr.net/npm/mupdf@1.28.0/dist/mupdf.js'),doc=mupdf.PDFDocument.openDocument(new Uint8Array(data),'application/pdf'),details=[];let removed=0;try{for(const e of wanted){if(e.page<1||e.page>doc.countPages())continue;const page=doc.loadPage(e.page-1),s=scanPage(page),ms=s.candidates.filter(c=>c.pathCount===Number(e.cloud.vectorAdditiveFamilyPathCount||0)&&c.curveCount===Number(e.cloud.vectorAdditiveFamilyCurveCount||0)&&c.lineCount===Number(e.cloud.vectorAdditiveFamilyLineCount||0)&&close(c.bbox,e.cloud.bbox));if(ms.length!==1){details.push({removed:false,page:e.page,reason:`${SRC}: revalidación=${ms.length}`});continue}const c=ms[0],st=s.streams[c.refIndex];st.ref.writeStream(toBytes(st.text.slice(0,c.start)+'\n'+st.text.slice(c.end)));removed++;details.push({removed:true,page:e.page,source:SRC,paths:c.pathCount,curves:c.curveCount,lines:c.lineCount})}if(!removed)return{data:new Uint8Array(data),removed:0,details};const b=doc.saveToBuffer('garbage=4,compress=yes,appearance=yes');return{data:new Uint8Array(b.asUint8Array()),removed,details}}catch(err){return{data:new Uint8Array(data),removed:0,details:[...details,{removed:false,reason:err?.message||String(err)}]}}finally{doc.destroy()}}
export async function detectAdditiveRevisionCloudFamilies(data,context={}){return mergeDedup(await detectV16(data,context),await detectNew(data))}
export async function removeAdditiveRevisionCloudFamilies(data,pages,options={}){const first=await removeNew(data,pages);const second=await removeV16(first.data,pages,options);return{data:second.data,removed:Number(first.removed||0)+Number(second.removed||0),details:[...(first.details||[]),...(second.details||[])]}}
