
### kind==='hybrid' 67449
start:t.start,end:t.end})}const byRef=new Map();for(const e of edits.values())(byRef.get(e.refKey)||byRef.set(e.refKey,{ref:e.ref,ranges:[]}).get(e.refKey)).ranges.push([e.start,e.end]);let touched=0;for(const g of byRef.values()){let text=latin(g.ref.readStream());for(const[a,b]of g.ranges.sort((A,B)=>B[0]-A[0])){if(a<0||b>text.length||a>=b)throw new Error('rango local inválido en stream interno');text=text.slice(0,a)+text.slice(b)}g.ref.writeStream(raw(text));touched++}if(!touched)throw new Error('no se encontró ningún stream editable');wp.destroy?.();wp=work.loadPage(0);const previewVisual=collectVisual(wp),previewModel=deepModel(wp);if(!previewModel||previewModel.incomplete||previewModel.strokes.length!==beforeStruct-selectedIdx.length)throw new Error('antes de guardar, el modelo estructural no equivale a original menos operadores seleccionados');if(!localBagOK(previewVisual)){if(!firstMismatchDiagnostic){const baseBag=new Map(),afterBag=new Map(),blueBag=new Map();for(const k of baselineSigs)baseBag.set(k,(baseBag.get(k)||0)+1);for(const v of previewVisual){const k=sig(v);afterBag.set(k,(afterBag.get(k)||0)+1)}for(const oi of selectedOrdinals){const k=baselineSigs[oi];blueBag.set(k,(blueBag.get(k)||0)+1)}let removed=0,added=0,blueRemoved=0,unselectedRemoved=0,blueStill=0,removedKinds=0,addedKinds=0;for(const[k,n]of baseBag){const d=Math.max(0,n-(afterBag.get(k)||0));if(d){removed+=d;removedKinds++;const bn=blueBag.get(k)||0;blueRemoved+=Math.min(d,bn);unselectedRemoved+=Math.max(0,d-bn)}}for(const[k,n]of afterBag){const d=Math.max(0,n-(baseBag.get(k)||0));if(d){added+=d;addedKinds++}}for(const[k,n]of blueBag){const d=Math.max(0,n-Math.max(0,(baseBag.get(k)||0)-(afterBag.get(k)||0)));blueStill+=d}firstMismatchDiagnostic='esperados azul='+selectedVisual+' · desaparecieron='+removed+' · azules realmente desaparecidos='+blueRemoved+' · no azules desaparecidos='+unselectedRemoved+' · azules que siguen presentes='+blueStill+' · firmas nuevas='+added+' · tipos desaparecidos='+removedKinds+' · tipos nuevos='+addedKinds+' · visual antes/después='+baselineVisual.length+'/'+previewVisual.length}throw new Error('antes de guardar, la geometría editada no coincide exactamente con la copia base menos azul')}const previewRaster=await rasterDigest(wp);if(saveMode==='incremental'&&work.canBeSavedIncrementally?.()===false)throw new Error('MuPDF indica que este PDF no admite guardado incremental');const outBuf=work.saveToBuffer(saveMode),out=new Uint8Array(U(outBuf));wp.destroy?.();wp=null;work.destroy();work=null;check=mupdf.PDFDocument.openDocument(out,'application/pdf');cp=check.loadPage(0);const afterRaster=await rasterDigest(cp);if(afterRaster!==previewRaster)throw new Error('después de guardar, el renderizado cambió respecto de la versión geométricamente validada');return{out,touched,number,saveMode:saveMode||'reescritura mínima'}}finally{try{cp?.destroy?.()}catch(_){}try{check?.destroy?.()}catch(_){}try{wp?.destroy?.()}catch(_){}try{work?.destroy?.()}catch(_){}}}
 let instancePlan=null,hybridPlan=null;try{status.textContent='Preparando borrado exacto de lo azul…';const causal=await buildCausalRoute();if(Array.isArray(causal)){const ck=causal.join(',');if(!keys.has(ck)){keys.add(ck);routes.unshift(causal)}causalDiagnostic=(causalDiagnostic?causalDiagnostic+' · ':'')+'RUTA CAUSAL COMPLETA'}else if(causal?.kind==='instance'){instancePlan=causal;causalDiagnostic=(causalDiagnostic?causalDiagnostic+' · ':'')+'INSTANCIA AISLADA LISTA'}else if(causal?.kind==='hybrid'){hybridPlan=causal;causalDiagnostic=(causalDiagnostic?causalDiagnostic+' · ':'')+'RUTA HÍBRIDA OPERADORES+ANOTACIÓN LISTA'}}catch(e){causalDiagnostic=(causalDiagnostic?causalDiagnostic+' · ':'')+'causal no completa: '+e.message}const saveModes=['incremental',''];if(hybridPlan)for(let sm=0;sm<saveModes.length;sm++){const saveMode=saveModes[sm],label=saveMode||'reescritura mínima';status.textContent='Verificando azul exacto · operadores + anotación · guardado '+label+'…';try{const ok=await attemptHybrid(hybridPlan,1,saveMode);if(ok){activeBytes=ok.out;doc.destroy();doc=mupdf.PDFDocument.openDocument(activeBytes,'application/pdf');pending=true;await renderPage();status.textContent='Selección eliminada y verificada: '+selectedVisual+' trazos azules · ruta causal híbrida · guardado '+ok.saveMode+'. Puedes continuar o guardar.';return}}catch(e){errors.push('ruta híbrida · guardado '+label+' · '+e.message)}}if(instancePlan)for(let sm=0;sm<saveModes.length;sm++){const saveMode=saveModes[sm],label=saveMode||'reescritura mínima';status.textContent='Verificando aislamiento exacto · guardado '+label+' · '+selectedVisual+' trazos azules…';try{const ok=await attemptInstance(instancePlan,1,saveMode);if(ok){activeBytes=ok.out;doc.destroy();doc=mupdf.PDFDocument.openDocument(activeBytes,'application/pdf');pending=true;await renderPage();status.textContent='Selección eliminada y verificada: '+selectedVisual+' trazos azules · aislamiento de instancia · guardado '+ok.saveMode+'. Puedes continuar o guardar.';return}}catch(e){errors.push('aislamiento de instancia · guardado '+label+' · '+e.message)}}for(let i=0;i<routes.length;i++)for(let sm=0;sm<saveModes.length;sm++){const saveMode=saveModes[sm],label=saveMode||'reescritura mínima';status.textContent='Verificando eliminación exacta · correspondencia '+(i+1)+'/'+routes.length+' · guardado '+label+' · '+selectedVisual+' trazos azules…';try{const ok=await attempt(routes[i],i+1,saveMode);if(ok){activeBytes=ok.out;doc.destroy();doc=mupdf.PDFDocument.openDocument(activeBytes,'application/pdf');pending=true;await renderPage();status.textContent='Selección eliminada y verificada: '+selectedVisual+' trazos azules · correspondencia '+ok.number+'/'+routes.length+' · guardado '+ok.saveMode+' · '+ok.touched+' stream(s). Puedes continuar o guardar.';return}}catch(e){errors.push('correspondencia '+(i+1)+' · guardado '+label+' · '+e.message)}}
 status.textContent='Borrado cancelado sin modificar el PDF activo: se probaron '+routes.length+' correspondencia(s) con '+saveModes.length+' estrategias de guardado y ninguna pasó la verificación exacta · '+errors.join(' | ')+(causalDiagnostic?' · CAUSAL: '+causalDiagnostic:'')+(firstMismatchDiagnostic?' · DIAGNÓSTICO: '+firstMismatchDiagnostic:'')}
$('#open').addEventListener('click',async()=>{try{await openPdf()}catch(e){if(e?.name!=='AbortError')status.textContent='Error: '+e.message}});$('#select').addEventListener('click',()=>status.textContent='Haz clic para reemplazar la selección. Ctrl+clic o Shift+clic añade otra familia.');$('#delete').addEventListener('click',removeGroup);$('#save').addEventListener('click',async()=>{if(!handle||!pending||!doc)return;let w=null;try{w=await handle.createWritable();if(!activeBytes)throw new Error('No hay una copia activa del PDF para guardar.');await w.write(activeBytes);await w.close();w=null;pending=false;setButtons();status.textContent='Cambios guardados sobre el archivo original.'}catch(e){try{await w?.abort?.()}catch(_){}status.textContent='Error al guardar: '+e.message}});overlay.addEventListener('click',chooseClassic);$('#zoom').addEventListener('input',e=>{setZoomAt(Number(e.target.value)/100);scheduleSharp()});$('#plus').addEventListener('click',()=>{setZoomAt(zoom*1.25);scheduleSharp()});$('#minus').addEventListener('click',()=>{setZoomAt(zoom/1.25);scheduleSharp()});$('#fit').addEventListener('click',()=>{fit();scheduleSharp(220)});$('#z100').addEventListener('click',()=>{setZoomAt(1);scheduleSharp()});viewport.addEventListener('wheel',e=>{if(!doc)return;e.preventDefault();const r=viewport.getBoundingClientRect();setZoomAt(zoom*Math.exp(-e.deltaY*.0017),e.clientX-r.left,e.clientY-r.top);scheduleSharp()},{passive:false});let dragging=false,lastX=0,lastY=0;viewport.addEventListener('mousedown',e=>{if(e.button!==1)return;e.preventDefault();clearDetail();dragging=true;lastX=e.clientX;lastY=e.clientY;viewport.classList.add('pan')});window.addEventListener('mousemove',e=>{if(!dragging)return;panX+=e.clientX-lastX;panY+=e.clientY-lastY;lastX=e.clientX;lastY=e.clientY;applyTransform()});window.addEventListener('mouseup',e=>{if(e.button===1){dragging=false;viewport.classList.remove('pan');scheduleSharp(120)}});viewport.addEventListener('auxclick',e=>{if(e.button===1)e.preventDefault()});window.addEventListener('resize',()=>{sizeOverlay();if(doc&&zoom<1.05)fit()});sizeOverlay();
</script><script id="v63-premium-ui">(function(){const main=document.querySelector('.main'),st=document.getElementById('status'),vp=document.getElementById('viewport');if(!main||!st||!vp)return;const dock=document.createElement('div');dock.className='bottomDock';const card=document.createElement('section');card.className='selectionCard';const glyph=document.createElement('div');glyph.className='selectionGlyph';glyph.textContent='◫';const text=document.createElement('div');const title=document.createElement('div');title.className='selectionTitle';title.textContent='Selección acumulada';text.append(title,st);const badge=document.createElement('div');badge.id='readyBadge';badge.className='readyBadge';badge.textContent='SIN SELECCIÓN';card.append(glyph,text,badge);const help=document.createElement('aside');help.className='helpCard';help.innerHTML='<div class="helpTitle">Ayuda y seguridad</div><div class="helpRow"><span class="key">Ctrl / Shift</span> + clic añade otra familia</div><div class="helpRow">Clic normal reemplaza la selección actual</div><div class="security"><span>Seguridad del borrado</span><strong id="safetyText">Bloqueado</strong></div>';dock.append(card,help);main.append(dock);const safe=document.getElementById('safetyText'),sync=()=>{const t=st.textContent||'';if(t.includes('LISTO PARA ELIMINAR')){badge.textContent='✓ LISTO PARA ELIMINAR';badge.className='readyBadge ok';if(safe){safe.textContent='Alta';safe.style.color='#35dc78'}}else if(t.includes('borrado bloqueado')||t.includes('Borrado bloqueado')||t.includes('Borrado cancelado')){badge.textContent='BORRADO BLOQUEADO';badge.className='readyBadge bad';if(safe){safe.textContent='Bloqueado';safe.style.color='#ff8c92'}}else{badge.textContent='SIN SELECCIÓN';badge.className='readyBadge';if(safe){safe.textContent='Bloqueado';safe.style.color='#93a7bc'}}};new MutationObserver(sync).observe(st,{childList:true,subtree:true,characterData:true});sync()})();</script></body></html>


### attemptHybrid 57680
ber,saveMode){let work=null,wp=null,check=null,cp=null;try{work=mupdf.PDFDocument.openDocument(outBase,'application/pdf');wp=work.loadPage(0);const baseline=collectVisual(wp),baseSigs=baseline.map(sig),wm=deepModel(wp);if(baseline.length!==beforeVisual||!wm||wm.incomplete||wm.strokes.length!==beforeStruct)throw new Error('la copia de instancia no reproduce el modelo base');const selectedSet=new Set(selectedOrdinals),expectedFinal=new Map();for(let i=0;i<baseline.length;i++){if(selectedSet.has(i))continue;const k=baseSigs[i];expectedFinal.set(k,(expectedFinal.get(k)||0)+1)}const bagEq=(a,exp,len)=>{if(a.length!==len)return false;const b=new Map();for(const v of a){const k=sig(v);b.set(k,(b.get(k)||0)+1)}if(b.size!==exp.size)return false;for(const[k,n]of exp)if(b.get(k)!==n)return false;return true};const regular=plan.baseRoute.filter(i=>i!==plan.offender),activeRegular=regular.map(i=>model.strokes[i]),targets=remapStructTargets(activeRegular,wm);if(targets.some(x=>!x||x.editable===false))throw new Error('la ruta base contiene un operador no editable');const edits=new Map();for(const t of targets){const k=deepKey(t.sourceRef)+'|'+t.start+'|'+t.end;if(!edits.has(k))edits.set(k,{ref:t.sourceRef,key:deepKey(t.sourceRef),start:t.start,end:t.end})}const groups=new Map();for(const e of edits.values())(groups.get(e.key)||groups.set(e.key,{ref:e.ref,ranges:[]}).get(e.key)).ranges.push([e.start,e.end]);for(const g of groups.values()){let text=latin(g.ref.readStream());for(const[a,b]of g.ranges.sort((A,B)=>B[0]-A[0]))text=text.slice(0,a)+text.slice(b);g.ref.writeStream(raw(text))}wp.destroy?.();wp=work.loadPage(0);const intermediate=collectVisual(wp),expInter=new Map(expectedFinal);expInter.set(plan.missingSig,(expInter.get(plan.missingSig)||0)+1);if(!bagEq(intermediate,expInter,beforeVisual-selectedVisual+1))throw new Error('la ruta base menos la pareja incorrecta no deja exactamente original menos 825 azules');const wm2=deepModel(wp),candidates=[];const missOrdinal=selectedOrdinals.find(i=>sig(classicVisual[i])===plan.missingSig),targetV=classicVisual[missOrdinal];for(const st of wm2.strokes){if(!st?.invocations?.length||st.sourceBase!==plan.fingerprint.sourceBase||invocationShape(st)!==plan.fingerprint.shape)continue;const c=alignCost(targetV,st);if(Number.isFinite(c))candidates.push({st,c})}candidates.sort((a,b)=>a.c-b.c);if(!candidates.length)throw new Error('después de borrar los demás azules no pude reencontrar la instancia aislable');isolateInstanceStroke(work,wp,candidates[0].st);wp.destroy?.();wp=work.loadPage(0);const preview=collectVisual(wp);if(!bagEq(preview,expectedFinal,beforeVisual-selectedVisual))throw new Error('la cirugía por instancia no produjo exactamente original menos azul');const previewRaster=await rasterDigest(wp);if(saveMode==='incremental'&&work.canBeSavedIncrementally?.()===false)throw new Error('MuPDF indica que este PDF no admite guardado incremental');const outBuf=work.saveToBuffer(saveMode),out=new Uint8Array(U(outBuf));wp.destroy?.();wp=null;work.destroy();work=null;check=mupdf.PDFDocument.openDocument(out,'application/pdf');cp=check.loadPage(0);if(await rasterDigest(cp)!==previewRaster)throw new Error('después de guardar, el renderizado cambió respecto de la versión exacta validada');return{out,touched:groups.size+1,number,saveMode:saveMode||'reescritura mínima'}}finally{try{cp?.destroy?.()}catch(_){}try{check?.destroy?.()}catch(_){}try{wp?.destroy?.()}catch(_){}try{work?.destroy?.()}catch(_){}}}
async function attemptHybrid(plan,number,saveMode){let work=null,wp=null,check=null,cp=null;try{work=mupdf.PDFDocument.openDocument(outBase,'application/pdf');wp=work.loadPage(0);const baseline=collectVisual(wp),baselineSigs=baseline.map(sig),wm=deepModel(wp);if(baseline.length!==beforeVisual||!wm||wm.incomplete||wm.strokes.length!==beforeStruct)throw new Error('la copia híbrida no reproduce el modelo base');const activeSigs=classicVisual.map(sig);for(let i=0;i<beforeVisual;i++)if(baselineSigs[i]!==activeSigs[i])throw new Error('la copia híbrida cambió el orden/firma visual en el trazo '+i);const selectedSet=new Set(selectedOrdinals),expectedFinal=new Map();for(let i=0;i<baseline.length;i++){if(selectedSet.has(i))continue;const k=baselineSigs[i];expectedFinal.set(k,(expectedFinal.get(k)||0)+1)}const bagEq=a=>{if(a.length!==beforeVisual-selectedVisual)return false;const b=new Map();for(const v of a){const k=sig(v);b.set(k,(b.get(k)||0)+1)}if(b.size!==expectedFinal.size)return false;for(const[k,n]of expectedFinal)if(b.get(k)!==n)return false;return true};const buckets=new Map();for(const s of wm.strokes){const k=structStableKey(s);(buckets.get(k)||buckets.set(k,[]).get(k)).push(s)}const targets=[];for(const k of plan.structKeys){const hits=buckets.get(k)||[];if(hits.length!==1)throw new Error('la identidad estructural híbrida no es única/estable para '+k+' · coincidencias='+hits.length);targets.push(hits[0])}const edits=new Map();for(const t of targets){if(!t||t.editable===false)throw new Error('la ruta híbrida contiene un operador no editable');const k=deepKey(t.sourceRef)+'|'+t.start+'|'+t.end;if(!edits.has(k))edits.set(k,{ref:t.sourceRef,refKey:deepKey(t.sourceRef),start:t.start,end:t.end})}const byRef=new Map();for(const e of edits.values())(byRef.get(e.refKey)||byRef.set(e.refKey,{ref:e.ref,ranges:[]}).get(e.refKey)).ranges.push([e.start,e.end]);for(const g of byRef.values()){let text=latin(g.ref.readStream());for(const[a,b]of g.ranges.sort((A,B)=>B[0]-A[0]))text=text.slice(0,a)+text.slice(b);g.ref.writeStream(raw(text))}try{wp?.destroy?.()}catch(_){}wp=work.loadPage(0);for(const ap of plan.annotations){const anns=wp.getAnnotations?.()||[];let hits=[];if(ap.objKey)hits=anns.filter(a=>{try{return deepKey(a.getObject?.())===ap.objKey}catch(_){return false}});if(hits.length!==1&&Number.isInteger(ap.index)&&anns[ap.index])hits=[anns[ap.index]];if(hits.length!==1)throw new Error('la anotación causal azul no pudo localizarse de forma única');if(typeof wp.deleteAnnotation==='function')wp.deleteAnnotation(hits[0]);else if(typeof wp.delete==='function')wp.delete(hits[0]);else throw new Error('MuPDF no expone eliminación de anotaciones en esta página');}try{wp?.destroy?.()}catch(_){}wp=work.loadPage(0);const preview=collectVisual(wp);if(!bagEq(preview))throw new Error('antes de guardar, la ruta híbrida no coincide exactamente con la copia base menos azul');const previewRaster=await rasterDigest(wp);if(saveMode==='incremental'&&work.canBeSavedIncrementally?.()===false)throw new Error('MuPDF indica que este PDF no admite guardado incremental');const outBuf=work.saveToBuffer(saveMode),out=new Uint8Array(U(outBuf));try{wp?.destroy?.()}catch(_){}wp=null;work.destroy();work=null;check=mupdf.PDFDocument.openDocument(out,'application/pdf');cp=check.loadPage(0);if(await rasterDigest(cp)!==previewRaster)throw new Error('después de guardar, el renderizado cambió respecto de la versión exacta validada');return{out,touched:byRef.size+plan.annotations.length,number,saveMode:saveMode||'reescritura mínima'}}finally{try{cp?.destroy?.()}catch(_){}try{check?.destroy?.()}catch(_){}try{wp?.destroy?.()}catch(_){}try{work?.destroy?.()}catch(_){}}}
async function attempt(selectedIdx,number,saveMode){let work=null,wp=null,check=null,cp=null;try{work=mupdf.PDFDocument.openDocument(outBase,'application/pdf');wp=work.loadPage(0);const baselineVisual=collectVisual(wp),wm=deepModel(wp);if(baselineVisual.length!==beforeVisual)throw new Error('la copia directa no reproduce el mismo número de trazos visuales ('+beforeVisual+' → '+baselineVisual.length+')');const baselineSigs=baselineVisual.map(sig),activeSigs=classicVisual.map(sig);for(let bi=0;bi<beforeVisual;bi++)if(baselineSigs[bi]!==activeSigs[bi])throw new Error('la copia directa cambió el orden/firma visual en el trazo '+bi);const selectedSet=new Set(selectedOrdinals),localExpected=new Map();for(let bi=0;bi<baselineVisual.length;bi++){if(selectedSet.has(bi))continue;const k=baselineSigs[bi];localExpected.set(k,(localExpected.get(k)||0)+1)}const localBagOK=a=>{if(a.length!==beforeVisual-selectedVisual)return false;const b=new Map();for(const v of a){const k=sig(v);b.set(k,(b.get(k)||0)+1)}if(b.size!==localExpected.size)return false;for(const[k,n]of localExpected)if(b.get(k)!==n)return false;return true};if(!wm||wm.incomplete||wm.strokes.length!==beforeStruct)throw new Error('el modelo profundo de la copia no coincide con el modelo activo');let structuralOrderDrift=0;for(let si=0;si<beforeStruct;si++)if(structStableKey(wm.strokes[si])!==structStableKey(model.strokes[si])){structuralOrderDrift++;if(structuralOrderDrift===1)console.warn('orden estructural distinto en',si,structStableKey(model.strokes[si]),structStableKey(wm.strokes[si]))}const activeTargets=selectedIdx.map(i=>model.strokes[i]);if(activeTargets.some(x=>!x||x.editable===false))throw new Error('hay operadores candidatos no editables con seguridad en el modelo activo');const targets=remapStructTargets(activeTargets,wm);if(targets.some(x=>!x||x.editable===false))throw new Error('hay operadores candidatos no editables con seguridad en la copia');const selectedBySource=new Map(),allBySource=new Map();for(const t of targets)(selectedBySource.get(t.sourceKey)||selectedBySource.set(t.sourceKey,[]).get(t.sourceKey)).push(t);for(const q of wm.strokes)(allBySource.get(q.sourceKey)||allBySource.set(q.sourceKey,[]).get(q.sourceKey)).push(q);for(const[k,a]of selectedBySource){const all=allBySource.get(k)||[];if(a.length!==all.length)throw new Error('XObject compartido: un operador candidato también produce geometría no seleccionada')}const edits=new Map();for(const t of targets){const k=deepKey(t.sourceRef)+'|'+t.start+'|'+t.end;if(!edits.has(k))edits.set(k,{ref:t.sourceRef,refKey:deepKey(t.sourceRef),start:t.start,end:t.end})}const byRef=new Map();for(const e of edits.values())(byRef.get(e.refKey)||byRef.set(e.refKey,{ref:e.ref,ranges:[]}).get(e.refKey)).ranges.push([e.start,e.end]);let touched=0;for(const g of byRef.values()){let text=latin(g.ref.readStream());for(const[a,b]of g.ranges.sort((A,B)=>B[0]-A[0])){if(a<0||b>text.length||a>=b)throw new Error('rango local inválido en stream interno');text=text.slice(0,a)+text.slice(b)}g.ref.writeStream(raw(text));touched++}if(!touched)throw new Error('no se encontró ningún stream editable');wp.destroy?.();wp=work.loadPage(0);const previewVisual=collectVisual(wp),previewModel=deepModel(wp);if(!previewModel||previewModel.incomplete||previewModel.strokes.length!==beforeStruct-selectedIdx.length)throw new Error('antes de guardar, el modelo estructural no equivale a original menos operadores seleccionados');if(!localBagOK(previewVisual)){if(!firstMismatchDiagnostic){const baseBag=new Map(),afterBag=new Map(),blueBag=new Map();for(const k of baselineSigs)baseBag.set(k,(baseBag.get(k)||0)+1);for(const v of previewVisual){const k=sig(v);afterBag.set(k,(afterBag.get(k)||0)+1)}for(const oi of selectedOrdinals){const k=baselineSigs[oi];blueBag.set(k,(blueBag.get(k)||0)+1)}let removed=0,added=0,blueRemoved=0,unselectedRemoved=0,blueStill=0,removedKinds=0,addedKinds=0;for(const[k,n]of baseBag){const d=Math.max(0,n-(afterBag.get(k)||0));if(d){removed+=d;removedKinds++;const bn=blueBag.get(k)||0;blueRemoved+=Math.min(d,bn);unselectedRemoved+=Math.max(0,d-bn)}}for(const[k,n]of afterBag){const d=Math.max(0,n-(baseBag.get(k)||0));if(d){added+=d;addedKinds++}}for(const[k,n]of blueBag){const d=Math.max(0,n-Math.max(0,(baseBag.get(k)||0)-(afterBag.get(k)||0)));blueStill+=d}f

### hybridPlan 66982
wm.strokes)(allBySource.get(q.sourceKey)||allBySource.set(q.sourceKey,[]).get(q.sourceKey)).push(q);for(const[k,a]of selectedBySource){const all=allBySource.get(k)||[];if(a.length!==all.length)throw new Error('XObject compartido: un operador candidato también produce geometría no seleccionada')}const edits=new Map();for(const t of targets){const k=deepKey(t.sourceRef)+'|'+t.start+'|'+t.end;if(!edits.has(k))edits.set(k,{ref:t.sourceRef,refKey:deepKey(t.sourceRef),start:t.start,end:t.end})}const byRef=new Map();for(const e of edits.values())(byRef.get(e.refKey)||byRef.set(e.refKey,{ref:e.ref,ranges:[]}).get(e.refKey)).ranges.push([e.start,e.end]);let touched=0;for(const g of byRef.values()){let text=latin(g.ref.readStream());for(const[a,b]of g.ranges.sort((A,B)=>B[0]-A[0])){if(a<0||b>text.length||a>=b)throw new Error('rango local inválido en stream interno');text=text.slice(0,a)+text.slice(b)}g.ref.writeStream(raw(text));touched++}if(!touched)throw new Error('no se encontró ningún stream editable');wp.destroy?.();wp=work.loadPage(0);const previewVisual=collectVisual(wp),previewModel=deepModel(wp);if(!previewModel||previewModel.incomplete||previewModel.strokes.length!==beforeStruct-selectedIdx.length)throw new Error('antes de guardar, el modelo estructural no equivale a original menos operadores seleccionados');if(!localBagOK(previewVisual)){if(!firstMismatchDiagnostic){const baseBag=new Map(),afterBag=new Map(),blueBag=new Map();for(const k of baselineSigs)baseBag.set(k,(baseBag.get(k)||0)+1);for(const v of previewVisual){const k=sig(v);afterBag.set(k,(afterBag.get(k)||0)+1)}for(const oi of selectedOrdinals){const k=baselineSigs[oi];blueBag.set(k,(blueBag.get(k)||0)+1)}let removed=0,added=0,blueRemoved=0,unselectedRemoved=0,blueStill=0,removedKinds=0,addedKinds=0;for(const[k,n]of baseBag){const d=Math.max(0,n-(afterBag.get(k)||0));if(d){removed+=d;removedKinds++;const bn=blueBag.get(k)||0;blueRemoved+=Math.min(d,bn);unselectedRemoved+=Math.max(0,d-bn)}}for(const[k,n]of afterBag){const d=Math.max(0,n-(baseBag.get(k)||0));if(d){added+=d;addedKinds++}}for(const[k,n]of blueBag){const d=Math.max(0,n-Math.max(0,(baseBag.get(k)||0)-(afterBag.get(k)||0)));blueStill+=d}firstMismatchDiagnostic='esperados azul='+selectedVisual+' · desaparecieron='+removed+' · azules realmente desaparecidos='+blueRemoved+' · no azules desaparecidos='+unselectedRemoved+' · azules que siguen presentes='+blueStill+' · firmas nuevas='+added+' · tipos desaparecidos='+removedKinds+' · tipos nuevos='+addedKinds+' · visual antes/después='+baselineVisual.length+'/'+previewVisual.length}throw new Error('antes de guardar, la geometría editada no coincide exactamente con la copia base menos azul')}const previewRaster=await rasterDigest(wp);if(saveMode==='incremental'&&work.canBeSavedIncrementally?.()===false)throw new Error('MuPDF indica que este PDF no admite guardado incremental');const outBuf=work.saveToBuffer(saveMode),out=new Uint8Array(U(outBuf));wp.destroy?.();wp=null;work.destroy();work=null;check=mupdf.PDFDocument.openDocument(out,'application/pdf');cp=check.loadPage(0);const afterRaster=await rasterDigest(cp);if(afterRaster!==previewRaster)throw new Error('después de guardar, el renderizado cambió respecto de la versión geométricamente validada');return{out,touched,number,saveMode:saveMode||'reescritura mínima'}}finally{try{cp?.destroy?.()}catch(_){}try{check?.destroy?.()}catch(_){}try{wp?.destroy?.()}catch(_){}try{work?.destroy?.()}catch(_){}}}
 let instancePlan=null,hybridPlan=null;try{status.textContent='Preparando borrado exacto de lo azul…';const causal=await buildCausalRoute();if(Array.isArray(causal)){const ck=causal.join(',');if(!keys.has(ck)){keys.add(ck);routes.unshift(causal)}causalDiagnostic=(causalDiagnostic?causalDiagnostic+' · ':'')+'RUTA CAUSAL COMPLETA'}else if(causal?.kind==='instance'){instancePlan=causal;causalDiagnostic=(causalDiagnostic?causalDiagnostic+' · ':'')+'INSTANCIA AISLADA LISTA'}else if(causal?.kind==='hybrid'){hybridPlan=causal;causalDiagnostic=(causalDiagnostic?causalDiagnostic+' · ':'')+'RUTA HÍBRIDA OPERADORES+ANOTACIÓN LISTA'}}catch(e){causalDiagnostic=(causalDiagnostic?causalDiagnostic+' · ':'')+'causal no completa: '+e.message}const saveModes=['incremental',''];if(hybridPlan)for(let sm=0;sm<saveModes.length;sm++){const saveMode=saveModes[sm],label=saveMode||'reescritura mínima';status.textContent='Verificando azul exacto · operadores + anotación · guardado '+label+'…';try{const ok=await attemptHybrid(hybridPlan,1,saveMode);if(ok){activeBytes=ok.out;doc.destroy();doc=mupdf.PDFDocument.openDocument(activeBytes,'application/pdf');pending=true;await renderPage();status.textContent='Selección eliminada y verificada: '+selectedVisual+' trazos azules · ruta causal híbrida · guardado '+ok.saveMode+'. Puedes continuar o guardar.';return}}catch(e){errors.push('ruta híbrida · guardado '+label+' · '+e.message)}}if(instancePlan)for(let sm=0;sm<saveModes.length;sm++){const saveMode=saveModes[sm],label=saveMode||'reescritura mínima';status.textContent='Verificando aislamiento exacto · guardado '+label+' · '+selectedVisual+' trazos azules…';try{const ok=await attemptInstance(instancePlan,1,saveMode);if(ok){activeBytes=ok.out;doc.destroy();doc=mupdf.PDFDocument.openDocument(activeBytes,'application/pdf');pending=true;await renderPage();status.textContent='Selección eliminada y verificada: '+selectedVisual+' trazos azules · aislamiento de instancia · guardado '+ok.saveMode+'. Puedes continuar o guardar.';return}}catch(e){errors.push('aislamiento de instancia · guardado '+label+' · '+e.message)}}for(let i=0;i<routes.length;i++)for(let sm=0;sm<saveModes.length;sm++){const saveMode=saveModes[sm],label=saveMode||'reescritura mínima';status.textContent='Verificando eliminación exacta · correspondencia '+(i+1)+'/'+routes.length+' · guardado '+label+' · '+selectedVisual+' trazos azules…';try{const ok=await attempt(routes[i],i+1,saveMode);if(ok){activeBytes=ok.out;doc.destroy();doc=mupdf.PDFDocument.openDocument(activeBytes,'application/pdf');pending=true;await renderPage();status.textContent='Selección eliminada y verificada: '+selectedVisual+' trazos azules · correspondencia '+ok.number+'/'+routes.length+' · guardado '+ok.saveMode+' · '+ok.touched+' stream(s). Puedes continuar o guardar.';return}}catch(e){errors.push('correspondencia '+(i+1)+' · guardado '+label+' · '+e.message)}}
 status.textContent='Borrado cancelado sin modificar el PDF activo: se probaron '+routes.length+' correspondencia(s) con '+saveModes.length+' estrategias de guardado y ninguna pasó la verificación exacta · '+errors.join(' | ')+(causalDiagnostic?' · CAUSAL: '+causalDiagnostic:'')+(firstMismatchDiagnostic?' · DIAGNÓSTICO: '+firstMismatchDiagnostic:'')}
$('#open').addEventListener('click',async()=>{try{await openPdf()}catch(e){if(e?.name!=='AbortError')status.textContent='Error: '+e.message}});$('#select').addEventListener('click',()=>status.textContent='Haz clic para reemplazar la selección. Ctrl+clic o Shift+clic añade otra familia.');$('#delete').addEventListener('click',removeGroup);$('#save').addEventListener('click',async()=>{if(!handle||!pending||!doc)return;let w=null;try{w=await handle.createWritable();if(!activeBytes)throw new Error('No hay una copia activa del PDF para guardar.');await w.write(activeBytes);await w.close();w=null;pending=false;setButtons();status.textContent='Cambios guardados sobre el archivo original.'}catch(e){try{await w?.abort?.()}catch(_){}status.textContent='Error al guardar: '+e.message}});overlay.addEventListener('click',chooseClassic);$('#zoom').addEventListener('input',e=>{setZoomAt(Number(e.target.value)/100);scheduleSharp()});$('#plus').addEventListener('click',()=>{setZoomAt(zoom*1.25);scheduleSharp()});$('#minus').addEventListener('click',()=>{setZoomAt(zoom/1.25);scheduleSharp()});$('#fit').addEventListener('click',()=>{fit();scheduleSharp(220)});$('#z100').addEventListener('click',()=>{setZoomAt(1);scheduleSharp()});viewport.addEventListener('wheel',e=>{if(!doc)return;e.preventDefault();const r=viewport.getBoundingClientRect();setZoomAt(zoom*Math.exp(-e.deltaY*.0017),e.clientX-r.left,e.clientY-r.top);scheduleSharp()},{passive:false});let dragging=false,lastX=0,lastY=0;viewport.addEventListener('mousedown',e=>{if(e.button!==1)return;e.preventDefault();clearDetail();dragging=true;lastX=e.clientX;lastY=e.clientY;viewport.classList.add('pan')});window.addEventListener('mousemove',e=>{if(!dragging)return;panX+=e.clientX-lastX;panY+=e.clientY-lastY;lastX=e.clientX;lastY=e.clientY;applyTransform()});window.addEventListener('mouseup',e=>{if(e.button===1){dragging=false;viewport.classList.remove('pan');scheduleSharp(120)}});viewport.addEventListener('auxclick',e=>{if(e.button===1)e.preventDefault()});window.addEventListener('resize',()=>{sizeOverlay();if(doc&&zoom<1.05)fit()});sizeOverlay();
</script><script id="v63-premium-ui">(function(){const main=document.querySelector('.main'),st=document.getElementById('status'),vp=document.getElementById('viewport');if(!main||!st||!vp)return;const dock=document.createElement('div');dock.className='bottomDock';const card=document.createElement('section');card.className='selectionCard';const glyph=document.createElement('div');glyph.className='selectionGlyph';glyph.textContent='◫';const text=document.createElement('div');const title=document.createElement('div');title.className='selectionTitle';title.textContent='Selección acumulada';text.append(title,st);const badge=document.createElement('div');badge.id='readyBadge';badge.className='readyBadge';badge.textContent='SIN SELECCIÓN';card.append(glyph,text,badge);const help=document.createElement('aside');help.className='helpCard';help.innerHTML='<div class="helpTitle">Ayuda y seguridad</div><div class="helpRow"><span class="key">Ctrl / Shift</span> + clic añade otra familia</div><div class="helpRow">Clic normal reemplaza la selección actual</div><div class="security"><span>Seguridad del borrado</span><strong id="safetyText">Bloqueado</strong></div>';dock.append(card,help);main.append(dock);const safe=document.getElementById('safetyText'),sync=()=>{const t=st.textContent||'';if(t.includes('LISTO PARA ELIMINAR')){badge.textContent='✓ LISTO PARA ELIMINAR';badge.className='readyBadge ok';if(safe){safe.textContent='Alta';safe.style.color='#35dc78'}}else if(t.includes('borrado bloqueado')||t.includes('Borrado bloqueado')||t.includes('Borrado cancelado')){badge.textContent='BORRADO BLOQUEADO';badge.className='readyBadge bad';if(safe){safe.textContent='Bloqueado';safe.style.color='#ff8c92'}}else{badge.textContent='SIN SELECCIÓN';badge.className='readyBadge';if(safe){safe.textContent='Bloqueado';safe.style.color='#93a7bc'}}};new MutationObserver(sync).observe(st,{childList:true,subtree:true,characterData:true});sync()})();</script></body></html>


### annotations:annotationPlans 44916
ficits(),initialMissing=[...missing.values()].reduce((a,b)=>a+b,0);
  if(initialMissing){
   const missingTargets=[];for(const k of missing.keys()){const a=visualBySig.get(k)||[];if(a.length)missingTargets.push(a[0])}
   const ranked=[];for(let idx=0;idx<cm.strokes.length;idx++){if(testedSet.has(idx))continue;const st=cm.strokes[idx];if(!st||st.editable===false)continue;let c=Infinity;for(const v of missingTargets)c=Math.min(c,alignCost(v,st));ranked.push({idx,c:Number.isFinite(c)?c:1e99});if(idx%48===0){status.textContent='Preparando búsqueda causal exhaustiva · '+idx+'/'+cm.strokes.length+' operadores revisados…';await uiYield()}}
   ranked.sort((a,b)=>a.c-b.c);const totalUnique=ranked.length;
   for(let q=0;q<ranked.length;q++){missing=deficits();if(!missing.size)break;const beforeTested=tested;await testIdx(ranked[q].idx,'Búsqueda causal exhaustiva '+(q+1)+'/'+totalUnique+' operadores únicos');if(tested>beforeTested)expanded++;if(q%6===0)await uiYield()}
  }
  missing=deficits();let missingCount=[...missing.values()].reduce((a,b)=>a+b,0);const chosen=[],used=new Set();for(const[k,n]of need){const a=(bySig.get(k)||[]).filter(i=>!used.has(i));const take=Math.min(n,a.length);for(let q=0;q<take;q++){chosen.push(a[q]);used.add(a[q])}}
  const annotationPlans=[];let annotationTests=0,annotationCount=0,widgetCount=0;
  try{annotationCount=Number(cp.getAnnotations?.()?.length||0)}catch(_){}try{widgetCount=Number(cp.getWidgets?.()?.length||0)}catch(_){}
  if(missingCount&&annotationCount){
   for(let ai=0;ai<annotationCount&&missingCount;ai++){
    let td=null,tp=null;try{td=mupdf.PDFDocument.openDocument(outBase,'application/pdf');tp=td.loadPage(0);const anns=tp.getAnnotations?.()||[],ann=anns[ai];if(!ann)continue;let objKey='';try{objKey=deepKey(ann.getObject?.())}catch(_){}let type='';try{type=String(ann.getType?.()||'')}catch(_){}let rect=null;try{rect=ann.getRect?.()||ann.getBounds?.()||null}catch(_){}
     if(typeof tp.deleteAnnotation==='function')tp.deleteAnnotation(ann);else if(typeof tp.delete==='function')tp.delete(ann);else continue;
     try{tp?.destroy?.()}catch(_){}tp=td.loadPage(0);const av=collectVisual(tp),ab=new Map();for(const v of av){const k=sig(v);ab.set(k,(ab.get(k)||0)+1)}let removed=[],added=0;for(const[k,n]of baseBag){const d=Math.max(0,n-(ab.get(k)||0));for(let q=0;q<d;q++)removed.push(k)}for(const[k,n]of ab)added+=Math.max(0,n-(baseBag.get(k)||0));annotationTests++;
     if(removed.length===1&&added===0){const k=removed[0],left=missing.get(k)||0;if(left>0){annotationPlans.push({index:ai,objKey,type,rect,sig:k});if(left===1)missing.delete(k);else missing.set(k,left-1);missingCount--;}}
    }catch(_){}finally{try{tp?.destroy?.()}catch(_){}try{td?.destroy?.()}catch(_){}}
    status.textContent='Buscando geometría azul fuera de Contents · anotación '+(ai+1)+'/'+annotationCount+'…';await uiYield()
   }
  }
  causalDiagnostic='pool inicial='+pool.length+' · pruebas únicas totales='+tested+' · ampliadas únicas='+expanded+' · operadores 1:1='+oneToOne+' · anotaciones='+annotationCount+' · widgets='+widgetCount+' · anotaciones probadas='+annotationTests+' · cubiertos='+(selectedVisual-missingCount)+'/'+selectedVisual;
  if(!missingCount&&annotationPlans.length){const structKeys=chosen.map(i=>structStableKey(cm.strokes[i]));if(chosen.length+annotationPlans.length!==selectedVisual||new Set(structKeys).size!==structKeys.length)throw new Error('la ruta híbrida no quedó 1:1');return{kind:'hybrid',structRoute:chosen,structKeys,annotations:annotationPlans}}
  if(missingCount)throw new Error('faltan fuentes causales para '+missingCount+' trazo(s) azul(es) después de probar operadores y anotaciones disponibles · '+causalDiagnostic);
  if(chosen.length!==selectedVisual||new Set(chosen).size!==chosen.length)throw new Error('la ruta causal ampliada no quedó 1:1');
  return chosen;
 }finally{try{cp?.destroy?.()}catch(_){}try{cw?.destroy?.()}catch(_){}}
}
function cloneStreamDict(pdf,ref){const d=pdf.newDictionary(),s=deepObj(ref);s.forEach((v,k)=>{k=String(k);if(k!=='Length'&&k!=='Filter'&&k!=='DecodeParms')d.put(k,v)});return d}
function addCloneStream(pdf,ref,text){return pdf.addStream(raw(text),cloneStreamDict(pdf,ref))}
function uniqueXName(res,prefix='BlueIso'){const xd=deepObj(deepObj(res)?.get?.('XObject'));if(!xd?.isDictionary?.())throw new Error('la instancia no tiene diccionario XObject editable');for(let n=1;n<10000;n++){const k=prefix+n;if(!xd.get(k))return{k,xd}}throw new Error('no encontré un nombre libre para el XObject aislado')}
function invocationShape(s){return(s?.invocations||[]).map(x=>String(x.name||'')).join('>')}
function isolateInstanceStroke(pdf,page,target){if(!target?.invocations?.length)throw new Error('el trazo no está dentro de una instancia Form/XObject aislable');const leafText=latin(target.sourceRef.readStream());if(target.start<0||target.end>leafText.length||target.start>=target.end)throw new Error('rango inválido del trazo dentro del Form');let clone=addCloneStream(pdf,target.sourceRef,leafText.slice(0,target.start)+leafText.slice(target.end));const roots=new Set(refs(page).map(deepKey));for(let z=target.invocations.length-1;z>=0;z--){const inv=target.invocations[z];if(!inv.parentRef||!inv.res||inv.nameStart<0||inv.nameEnd<=inv.nameStart)throw new Error('la cadena de instancia no conserva un Do editable');const {k,xd}=uniqueXName(inv.res);xd.put(k,clone);const txt=latin(inv.parentRef.readStream());if(inv.nameEnd>txt.length)throw new Error('el Do de la instancia quedó fuera de rango');const mod=txt.slice(0,inv.nameStart)+'/'+k+txt.slice(inv.nameEnd);if(roots.has(deepKey(inv.parentRef))){inv.parentRef.writeStream(raw(mod));return true}clone=addCloneStream(pdf,inv.parentRef,mod)}throw new Error('la cadena de instancia no llegó a un stream raíz de página')}
async function buildLargeInstancePlan(){
 const baseRoute=routes[0]?.slice();if(!baseRoute||baseRoute.length!==selectedVisual)throw new Error('no existe una ruta base completa para reparación por instancia');
 let cw=null,cp=null;try{
  cw=mupdf.PDFDocument.openDocument(outBase,'application/pdf');cp=cw.loadPage(0);const base=collectVisual(cp),baseSigs=base.map(sig),cm=deepModel(cp);
  if(base.length!==beforeVisual||!cm||cm.incomplete||cm.strokes.length!==beforeStruct)throw new Error('la copia de reparación no reproduce el modelo base');
  const baseBag=new Map();for(const k of baseSigs)baseBag.set(k,(baseBag.get(k)||0)+1);const need=new Map(),blueVisualBySig=new Map();for(const oi of selectedOrdinals){const k=baseSigs[oi];need.set(k,(need.get(k)||0)+1);(blueVisualBySig.get(k)||blueVisualBySig.set(k,[]).get(k)).push(base[oi])}
  function diff(av){const ab=new Map();for(const v of av){const k=sig(v);ab.set(k,(ab.get(k)||0)+1)}const removed=new Map(),added=new Map();for(const[k,n]of baseBag){const d=Math.max(0,n-(ab.get(k)||0));if(d)removed.set(k,d)}for(const[k,n]of ab){const d=Math.max(0,n-(baseBag.get(k)||0));if(d)added.set(k,d)}return{removed,added}}
  async function routeDiff(indices){const groups=new Map();for(const idx of indices){const t=cm.strokes[idx];if(!t||t.editable===false)throw new Error('la ruta base contiene un operador no editable');const key=deepKey(t.sourceRef);let g=groups.get(key);if(!g){g={ref:t.sourceRef,original:latin(t.sourceRef.readStream()),ranges:[]};groups.set(key,g)}g.ranges.push([t.start,t.end])}try{for(const g of groups.values()){let text=g.original;for(const[a,b]of g.ranges.sort((A,B)=>B[0]-A[0]))text=text.slice(0,a)+text.slice(b);g.ref.writeStream(raw(text))}try{cp?.destroy?.()}catch(_){}cp=cw.loadPage(0);return diff(collectVisual(cp))}finally{for(const g of groups.values())g.ref.writeStream(raw(g.original));try{cp?.destroy?.()}catch(_){}cp=cw.loadPage(0)}}
  async function single(idx){const t=cm.strokes[idx];if(!t||t.editable===false)return null;const orig=latin(t.sourceRef.readStream());try{t.sourceRef.writeStream(raw(orig.slice(0,t.start)+orig.slice(t.end)));try{cp?.destroy?.()}catch(_){}cp=cw.loadPage(0);const d=diff(collectVisual(cp));let total=0,key=null;for(const[k,n]of d.removed){total+=n;if(n===1)key=k}return total===1&&d.added.size===0?key:null}finally{t.sourceRef.writeStream(raw(orig));try{cp?.destroy?.()}catch(_){}cp=cw.loadPage(0)}}
  status.textContent='Aislando exactamente lo azul…';const d=await routeDiff(baseRoute),missing=new Map(),extra=new Map();for(const[k,n]of need){const got=d.removed.get(k)||0;if(got<n)missing.set(k,n-got)}for(const[k,n]of d.removed){const bn=need.get(k)||0;if(n>bn)extra.set(k,n-bn)}const missCount=[...missing.values()].reduce((a,b)=>a+b,0),extraCount=[...extra.values()].reduce((a,b)=>a+b,0),addedCount=[...d.added.values()].reduce((a,b)=>a+b,0);causalDiagnostic='aislamiento de instancia · ruta='+selectedVisual+' · faltantes='+missCount+' · no azules='+extraCount+' · nuevas='+addedCount;if(missCount!==1||extraCount!==1||addedCount!==0)throw new Error('la desviación no es una sustitución 1↔1 aislable · '+causalDiagnostic);
  const missingSig=[...missing.keys()][0],extraSig=[...extra.keys()][0],missingV=(blueVisualBySig.get(missingSig)||[])[0],extraV=base.find(v=>sig(v)===extraSig);if(!missingV||!extraV)throw new Error('no pude recuperar la pareja visual 1↔1');
  const offenderRank=baseRoute.map(idx=>({idx,c:alignCost(extraV,cm.strokes[idx])})).filter(x=>Number.isFinite(x.c)).sort((a,b)=>a.c-b.c);let offender=-1,offTests=0;for(const r of offenderRank){const k=await single(r.idx);offTests++;if(k===extraSig){offender=r.idx;break}if(offTests%20===0){status.textContent='Aislando azul · localizando la única pareja incorrecta…';await uiYield()}}if(offender<0)throw new Error('no se localizó el operador que borra el trazo no azul');
  const rootRanked=[];for(let idx=0;idx<cm.strokes.length;idx++){if(baseRoute.includes(idx))continue;const st=cm.strokes[idx];if(!st||st.editable===false||st.invocations?.length)continue;const c=alignCost(missingV,st);if(Number.isFinite(c))rootRanked.push({idx,c})}rootRanked.sort((a,b)=>a.c-b.c);let rootReplacement=-1,rootTests=0;for(const r of rootRanked){const k=await single(r.idx);rootTests++;if(k===missingSig){rootReplacement=r.idx;break}if(rootTests%25===0){status.textContent='Borrado exacto · buscando el último azul en streams de página '+rootTests+'/'+rootRanked.length+'…';await uiYield()}}if(rootReplacement>=0){const repaired=baseRoute.map(i=>i===offender?rootReplacement:i);if(new Set(repaired).size!==repaired.length)throw new Error('la ruta raíz reparada contiene operadores duplicados');causalDiagnostic+=' · operador incorrecto='+offender+' · raíz exacta encontrada tras '+rootTests+'/'+rootRanked.length+' pruebas · ROOT-CAUSAL-EXHAUSTIVO';return repaired}causalDiagnostic+=' · búsqueda raíz exhaustiva='+rootTests+'/'+rootRanked.length+' sin coincidencia';
  const ranked=[];for(let idx=0;idx<cm.strokes.length;idx++){if(baseRoute.includes(idx))continue;const st=cm.strokes[idx];if(!st||st.editable===false||!st.invocations?.length)continue;const c=alignCost(missingV,st);if(Number.isFinite(c))ranked.push({idx,c})}ranked.sort((a,b)=>a.c-b.c);let replacement=-1,fingerprint=null,tests=0,max=Math.min(220,ranked.length);
  for(let q=0;q<max;q++){const idx=ranked[q].idx;let td=null,tp=null;try{td=mupdf.PDFDocument.openDocument(outBase,'application/pdf');tp=td.loadPage(0);const bv=collectVisual(tp),bm=deepModel(tp),bb=new Map();for(const v of bv){const k=sig(v);bb.set(k,(bb.get(k)||0)+1)}const st=bm.strokes[idx];if(!st?.invocations?.length)continue;isolateInstanceStroke(td,tp,st);tp.destroy?.();tp=td.loadPage

### RUTA CAUSAL COMPLETA 67268
cionada')}const edits=new Map();for(const t of targets){const k=deepKey(t.sourceRef)+'|'+t.start+'|'+t.end;if(!edits.has(k))edits.set(k,{ref:t.sourceRef,refKey:deepKey(t.sourceRef),start:t.start,end:t.end})}const byRef=new Map();for(const e of edits.values())(byRef.get(e.refKey)||byRef.set(e.refKey,{ref:e.ref,ranges:[]}).get(e.refKey)).ranges.push([e.start,e.end]);let touched=0;for(const g of byRef.values()){let text=latin(g.ref.readStream());for(const[a,b]of g.ranges.sort((A,B)=>B[0]-A[0])){if(a<0||b>text.length||a>=b)throw new Error('rango local inválido en stream interno');text=text.slice(0,a)+text.slice(b)}g.ref.writeStream(raw(text));touched++}if(!touched)throw new Error('no se encontró ningún stream editable');wp.destroy?.();wp=work.loadPage(0);const previewVisual=collectVisual(wp),previewModel=deepModel(wp);if(!previewModel||previewModel.incomplete||previewModel.strokes.length!==beforeStruct-selectedIdx.length)throw new Error('antes de guardar, el modelo estructural no equivale a original menos operadores seleccionados');if(!localBagOK(previewVisual)){if(!firstMismatchDiagnostic){const baseBag=new Map(),afterBag=new Map(),blueBag=new Map();for(const k of baselineSigs)baseBag.set(k,(baseBag.get(k)||0)+1);for(const v of previewVisual){const k=sig(v);afterBag.set(k,(afterBag.get(k)||0)+1)}for(const oi of selectedOrdinals){const k=baselineSigs[oi];blueBag.set(k,(blueBag.get(k)||0)+1)}let removed=0,added=0,blueRemoved=0,unselectedRemoved=0,blueStill=0,removedKinds=0,addedKinds=0;for(const[k,n]of baseBag){const d=Math.max(0,n-(afterBag.get(k)||0));if(d){removed+=d;removedKinds++;const bn=blueBag.get(k)||0;blueRemoved+=Math.min(d,bn);unselectedRemoved+=Math.max(0,d-bn)}}for(const[k,n]of afterBag){const d=Math.max(0,n-(baseBag.get(k)||0));if(d){added+=d;addedKinds++}}for(const[k,n]of blueBag){const d=Math.max(0,n-Math.max(0,(baseBag.get(k)||0)-(afterBag.get(k)||0)));blueStill+=d}firstMismatchDiagnostic='esperados azul='+selectedVisual+' · desaparecieron='+removed+' · azules realmente desaparecidos='+blueRemoved+' · no azules desaparecidos='+unselectedRemoved+' · azules que siguen presentes='+blueStill+' · firmas nuevas='+added+' · tipos desaparecidos='+removedKinds+' · tipos nuevos='+addedKinds+' · visual antes/después='+baselineVisual.length+'/'+previewVisual.length}throw new Error('antes de guardar, la geometría editada no coincide exactamente con la copia base menos azul')}const previewRaster=await rasterDigest(wp);if(saveMode==='incremental'&&work.canBeSavedIncrementally?.()===false)throw new Error('MuPDF indica que este PDF no admite guardado incremental');const outBuf=work.saveToBuffer(saveMode),out=new Uint8Array(U(outBuf));wp.destroy?.();wp=null;work.destroy();work=null;check=mupdf.PDFDocument.openDocument(out,'application/pdf');cp=check.loadPage(0);const afterRaster=await rasterDigest(cp);if(afterRaster!==previewRaster)throw new Error('después de guardar, el renderizado cambió respecto de la versión geométricamente validada');return{out,touched,number,saveMode:saveMode||'reescritura mínima'}}finally{try{cp?.destroy?.()}catch(_){}try{check?.destroy?.()}catch(_){}try{wp?.destroy?.()}catch(_){}try{work?.destroy?.()}catch(_){}}}
 let instancePlan=null,hybridPlan=null;try{status.textContent='Preparando borrado exacto de lo azul…';const causal=await buildCausalRoute();if(Array.isArray(causal)){const ck=causal.join(',');if(!keys.has(ck)){keys.add(ck);routes.unshift(causal)}causalDiagnostic=(causalDiagnostic?causalDiagnostic+' · ':'')+'RUTA CAUSAL COMPLETA'}else if(causal?.kind==='instance'){instancePlan=causal;causalDiagnostic=(causalDiagnostic?causalDiagnostic+' · ':'')+'INSTANCIA AISLADA LISTA'}else if(causal?.kind==='hybrid'){hybridPlan=causal;causalDiagnostic=(causalDiagnostic?causalDiagnostic+' · ':'')+'RUTA HÍBRIDA OPERADORES+ANOTACIÓN LISTA'}}catch(e){causalDiagnostic=(causalDiagnostic?causalDiagnostic+' · ':'')+'causal no completa: '+e.message}const saveModes=['incremental',''];if(hybridPlan)for(let sm=0;sm<saveModes.length;sm++){const saveMode=saveModes[sm],label=saveMode||'reescritura mínima';status.textContent='Verificando azul exacto · operadores + anotación · guardado '+label+'…';try{const ok=await attemptHybrid(hybridPlan,1,saveMode);if(ok){activeBytes=ok.out;doc.destroy();doc=mupdf.PDFDocument.openDocument(activeBytes,'application/pdf');pending=true;await renderPage();status.textContent='Selección eliminada y verificada: '+selectedVisual+' trazos azules · ruta causal híbrida · guardado '+ok.saveMode+'. Puedes continuar o guardar.';return}}catch(e){errors.push('ruta híbrida · guardado '+label+' · '+e.message)}}if(instancePlan)for(let sm=0;sm<saveModes.length;sm++){const saveMode=saveModes[sm],label=saveMode||'reescritura mínima';status.textContent='Verificando aislamiento exacto · guardado '+label+' · '+selectedVisual+' trazos azules…';try{const ok=await attemptInstance(instancePlan,1,saveMode);if(ok){activeBytes=ok.out;doc.destroy();doc=mupdf.PDFDocument.openDocument(activeBytes,'application/pdf');pending=true;await renderPage();status.textContent='Selección eliminada y verificada: '+selectedVisual+' trazos azules · aislamiento de instancia · guardado '+ok.saveMode+'. Puedes continuar o guardar.';return}}catch(e){errors.push('aislamiento de instancia · guardado '+label+' · '+e.message)}}for(let i=0;i<routes.length;i++)for(let sm=0;sm<saveModes.length;sm++){const saveMode=saveModes[sm],label=saveMode||'reescritura mínima';status.textContent='Verificando eliminación exacta · correspondencia '+(i+1)+'/'+routes.length+' · guardado '+label+' · '+selectedVisual+' trazos azules…';try{const ok=await attempt(routes[i],i+1,saveMode);if(ok){activeBytes=ok.out;doc.destroy();doc=mupdf.PDFDocument.openDocument(activeBytes,'application/pdf');pending=true;await renderPage();status.textContent='Selección eliminada y verificada: '+selectedVisual+' trazos azules · correspondencia '+ok.number+'/'+routes.length+' · guardado '+ok.saveMode+' · '+ok.touched+' stream(s). Puedes continuar o guardar.';return}}catch(e){errors.push('correspondencia '+(i+1)+' · guardado '+label+' · '+e.message)}}
 status.textContent='Borrado cancelado sin modificar el PDF activo: se probaron '+routes.length+' correspondencia(s) con '+saveModes.length+' estrategias de guardado y ninguna pasó la verificación exacta · '+errors.join(' | ')+(causalDiagnostic?' · CAUSAL: '+causalDiagnostic:'')+(firstMismatchDiagnostic?' · DIAGNÓSTICO: '+firstMismatchDiagnostic:'')}
$('#open').addEventListener('click',async()=>{try{await openPdf()}catch(e){if(e?.name!=='AbortError')status.textContent='Error: '+e.message}});$('#select').addEventListener('click',()=>status.textContent='Haz clic para reemplazar la selección. Ctrl+clic o Shift+clic añade otra familia.');$('#delete').addEventListener('click',removeGroup);$('#save').addEventListener('click',async()=>{if(!handle||!pending||!doc)return;let w=null;try{w=await handle.createWritable();if(!activeBytes)throw new Error('No hay una copia activa del PDF para guardar.');await w.write(activeBytes);await w.close();w=null;pending=false;setButtons();status.textContent='Cambios guardados sobre el archivo original.'}catch(e){try{await w?.abort?.()}catch(_){}status.textContent='Error al guardar: '+e.message}});overlay.addEventListener('click',chooseClassic);$('#zoom').addEventListener('input',e=>{setZoomAt(Number(e.target.value)/100);scheduleSharp()});$('#plus').addEventListener('click',()=>{setZoomAt(zoom*1.25);scheduleSharp()});$('#minus').addEventListener('click',()=>{setZoomAt(zoom/1.25);scheduleSharp()});$('#fit').addEventListener('click',()=>{fit();scheduleSharp(220)});$('#z100').addEventListener('click',()=>{setZoomAt(1);scheduleSharp()});viewport.addEventListener('wheel',e=>{if(!doc)return;e.preventDefault();const r=viewport.getBoundingClientRect();setZoomAt(zoom*Math.exp(-e.deltaY*.0017),e.clientX-r.left,e.clientY-r.top);scheduleSharp()},{passive:false});let dragging=false,lastX=0,lastY=0;viewport.addEventListener('mousedown',e=>{if(e.button!==1)return;e.preventDefault();clearDetail();dragging=true;lastX=e.clientX;lastY=e.clientY;viewport.classList.add('pan')});window.addEventListener('mousemove',e=>{if(!dragging)return;panX+=e.clientX-lastX;panY+=e.clientY-lastY;lastX=e.clientX;lastY=e.clientY;applyTransform()});window.addEventListener('mouseup',e=>{if(e.button===1){dragging=false;viewport.classList.remove('pan');scheduleSharp(120)}});viewport.addEventListener('auxclick',e=>{if(e.button===1)e.preventDefault()});window.addEventListener('resize',()=>{sizeOverlay();if(doc&&zoom<1.05)fit()});sizeOverlay();
</script><script id="v63-premium-ui">(function(){const main=document.querySelector('.main'),st=document.getElementById('status'),vp=document.getElementById('viewport');if(!main||!st||!vp)return;const dock=document.createElement('div');dock.className='bottomDock';const card=document.createElement('section');card.className='selectionCard';const glyph=document.createElement('div');glyph.className='selectionGlyph';glyph.textContent='◫';const text=document.createElement('div');const title=document.createElement('div');title.className='selectionTitle';title.textContent='Selección acumulada';text.append(title,st);const badge=document.createElement('div');badge.id='readyBadge';badge.className='readyBadge';badge.textContent='SIN SELECCIÓN';card.append(glyph,text,badge);const help=document.createElement('aside');help.className='helpCard';help.innerHTML='<div class="helpTitle">Ayuda y seguridad</div><div class="helpRow"><span class="key">Ctrl / Shift</span> + clic añade otra familia</div><div class="helpRow">Clic normal reemplaza la selección actual</div><div class="security"><span>Seguridad del borrado</span><strong id="safetyText">Bloqueado</strong></div>';dock.append(card,help);main.append(dock);const safe=document.getElementById('safetyText'),sync=()=>{const t=st.textContent||'';if(t.includes('LISTO PARA ELIMINAR')){badge.textContent='✓ LISTO PARA ELIMINAR';badge.className='readyBadge ok';if(safe){safe.textContent='Alta';safe.style.color='#35dc78'}}else if(t.includes('borrado bloqueado')||t.includes('Borrado bloqueado')||t.includes('Borrado cancelado')){badge.textContent='BORRADO BLOQUEADO';badge.className='readyBadge bad';if(safe){safe.textContent='Bloqueado';safe.style.color='#ff8c92'}}else{badge.textContent='SIN SELECCIÓN';badge.className='readyBadge';if(safe){safe.textContent='Bloqueado';safe.style.color='#93a7bc'}}};new MutationObserver(sync).observe(st,{childList:true,subtree:true,characterData:true});sync()})();</script></body></html>

