name: Build selector component seed expand
on:
  push:
    branches: [candidate/selector-component-seed-expand]
permissions:
  contents: write
jobs:
  build:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: candidate/selector-component-seed-expand
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Patch component seed expansion
        run: python tmp-component-seed-expand.py
      - name: Verify targeted code
        run: |
          grep -F "function seedComponentGroups" selector-nubes-multistream-core.html
          grep -F "expansión por componente" selector-nubes-multistream-core.html
          grep -F "componentLike(g,seed)" selector-nubes-multistream-core.html
      - name: JavaScript syntax check
        run: |
          python - <<'PY'
          from pathlib import Path
          import re
          s=Path('selector-nubes-multistream-core.html').read_text()
          m=re.search(r'<script type="module">(.*?)</script>',s,re.S)
          if not m: raise SystemExit('module script not found')
          js=m.group(1)
          js=re.sub(r"^import \* as mupdf from .*?;\s*", "const mupdf={};\n", js, count=1)
          Path('/tmp/selector.mjs').write_text(js)
          PY
          node --check /tmp/selector.mjs
      - name: Commit generated core
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add selector-nubes-multistream-core.html
          git diff --cached --quiet && exit 0
          git commit -m "Expand strict seeds to matching visual components"
          git push origin HEAD:candidate/selector-component-seed-expand
