import * as mupdf from 'https://cdn.jsdelivr.net/npm/mupdf@1.28.0/dist/mupdf.js';

const BOX = '#batchRemoveRevisionClouds';
const SUMMARY = '#batchSummary';
const STATUS = '#batchStatus';
const q = (s) => document.querySelector(s);
function isRed(r, g, b) { return r >= 165 && g <= 135 && b <= 135 && r >= g + 60 && r >= b + 60; }
function closeMask(mask, w, h) { const out = new Uint8Array(mask), near = new Uint8Array(mask); for (let y=1;y<h-1;y++) for (let x=1;x<w-1;x++){const i=y*w+x;if(mask[i])continue;if(mask[i-1]||mask[i+1]||mask[i-w]||mask[i+w])near[i]=1;} for(let i=0;i<out.length;i++)if(near[i])out[i]=1; return out; }
function findClouds(page, scale=0.35){const pix=page.toPixmap(mupdf.Matrix.scale(scale,scale),mupdf.ColorSpace.DeviceRGB,false,false),w=pix.getWidth(),h=pix.getHeight(),c=pix.getNumberOfComponents();if(!w||!h||c<3)return[];const px=pix.getPixels(),red=new Uint8Array(w*h);for(let i=0,p=0;i<red.length;i++,p+=c)red[i]=isRed(px[p],px[p+1],px[p+2])?1:0;const closed=closeMask(red,w,h),seen=new Uint8Array(w*h),stack=new Int32Array(w*h),b=page.getBounds(),sx=(b[2]-b[0])/w,sy=(b[3]-b[1])/h,found=[];for(let y0=0;y0<h;y0++)for(let x0=0;x0<w;x0++){const seed=y0*w+x0;if(!closed[seed]||seen[seed])continue;let top=0,area=0,minX=x0,maxX=x0,minY=y0,maxY=y0;stack[top++]=seed;seen[seed]=1;while(top){const idx=stack[--top],y=(idx/w)|0,x=idx-y*w;area++;if(x<minX)minX=x;if(x>maxX)maxX=x;if(y<minY)minY=y;if(y>maxY)maxY=y;const n1=idx-1,n2=idx+1,n3=idx-w,n4=idx+w;if(x>0&&closed[n1]&&!seen[n1]){seen[n1]=1;stack[top++]=n1;}if(x+1<w&&closed[n2]&&!seen[n2]){seen[n2]=1;stack[top++]=n2;}if(y>0&&closed[n3]&&!seen[n3]){seen[n3]=1;stack[top++]=n3;}if(y+1<h&&closed[n4]&&!seen[n4]){seen[n4]=1;stack[top++]=n4;}}const rw=maxX-minX+1,rh=maxY-minY+1,d=area/Math.max(1,rw*rh);if(area<350||rw<80||rh<80||d>0.10)continue;let rough=0,rows=0,prev=null;for(let y=minY;y<=maxY;y++){let lo=w,hi=-1;for(let x=minX;x<=maxX;x++)if(closed[y*w+x]){lo=Math.min(lo,x);hi=Math.max(hi,x);}if(hi>=0){rows++;if(prev)rough+=Math.abs(lo-prev[0])+Math.abs(hi-prev[1]);prev=[lo,hi];}}const rr=rough/Math.max(1,rows);if(rr<0.8)continue;found.push({pixelBox:[minX,minY,maxX+1,maxY+1],scale,red,closed,width:w,height:h,area,density:d,roughness:rr,pageBox:[b[0]+minX*sx,b[1]+minY*sy,b[0]+(maxX+1)*sx,b[1]+(maxY+1)*sy]});}return found.sort((a,z)=>z.area-a.area).slice(0,4);}
function runsForCloud(cloud){const{red,closed,width:w,height:h,pixelBox}=cloud,[minX,minY,maxX,maxY]=pixelBox,keep=new Uint8Array(red.length);for(let y=Math.max(0,minY-2);y<Math.min(h,maxY+2);y++)for(let x=Math.max(0,minX-2);x<Math.min(w,maxX+2);x++){const i=y*w+x;if(red[i]&&closed[i])keep[i]=1;}const runs=[];for(let y=minY;y<maxY;y++){let x=minX;while(x<maxX){while(x<maxX&&!keep[y*w+x])x++;if(x>=maxX)break;const s=x;while(x<maxX&&keep[y*w+x])x++;runs.push([s,y,x,y+1]);}}return runs;}
export async function detectRevisionClouds(data){const doc=mupdf.PDFDocument.openDocument(new Uint8Array(data),'application/pdf'),out=[];try{for(let i=0;i<doc.countPages();i++){const page=doc.loadPage(i),clouds=findClouds(page);if(clouds.length)out.push({page:i+1,clouds});}}finally{doc.destroy();}return out;}
export async function removeRevisionClouds(data,detected){if(!detected?.length)return{bytes:new Uint8Array(data),count:0};const doc=mupdf.PDFDocument.openDocument(new Uint8Array(data),'application/pdf');let count=0;try{for(const pi of detected){const page=doc.loadPage(pi.page-1),b=page.getBounds();for(const cloud of pi.clouds){const runs=runsForCloud(cloud),merged=[],last=new Map(),sx=(b[2]-b[0])/cloud.width,sy=(b[3]-b[1])/cloud.height;for(const r of runs){const k=`${r[0]}:${r[2]}`,prev=last.get(k);if(prev&&prev[3]===r[1])prev[3]=r[3];else{const m=[...r];merged.push(m);last.set(k,m);}}for(const r of merged){const a=page.createAnnotation('Redact');a.setRect([b[0]+r[0]*sx,b[1]+r[1]*sy,b[0]+r[2]*sx,b[1]+r[3]*sy]);try{a.setBorderWidth(0)}catch(_){}a.update();}if(merged.length){if(typeof page.applyRedactions!=='function')throw new Error('MuPDF no expone applyRedactions().');page.applyRedactions(false,0);page.update();count++;}}}const buf=doc.saveToBuffer('garbage=4,compress=yes,appearance=yes');return{bytes:new Uint8Array(buf.asUint8Array()),count};}finally{doc.destroy();}}
function inject(){if(q(BOX))return;const c=q('#batchRemoveComments'),host=c?.closest('.option-box');if(!host)return;const box=document.createElement('div');box.className='option-box';box.style.marginTop='10px';box.innerHTML='<label><input id="batchRemoveRevisionClouds" type="checkbox"><span>☁️ Eliminar nubes de revisión gráficas</span></label><small>Detecta automáticamente nubes gráficas rojas de revisión. Solo se ejecuta al activar esta opción.</small>';host.parentElement?.insertBefore(box,host.nextElementSibling);}
async function analyze(){if(!q(BOX)?.checked)return;const batch=window.__batchAnalysis||[];let total=0;for(const item of batch){if(item?.error||!item.data)continue;try{const d=await detectRevisionClouds(item.data);item.revisionClouds=d;item.revisionCloudCount=d.reduce((s,p)=>s+p.clouds.length,0);total+=item.revisionCloudCount;}catch(e){item.revisionClouds=[];item.revisionCloudError=e?.message||String(e);}}const s=q(SUMMARY);if(s){const base=(s.textContent||'').replace(/ · ☁️[^·]*/g,'').trim();s.textContent=total?`${base} · ☁️ ${total} nube${total===1?'':'s'} de revisión detectada${total===1?'':'s'}`:`${base} · ☁️ 0 nubes de revisión detectadas`;s.classList.remove('hidden');}if(q(STATUS))q(STATUS).textContent=total?`Detectadas ${total} nube${total===1?'':'s'} de revisión gráfica${total===1?'':'s'}`:'No se detectaron nubes de revisión gráficas.';}
async function prepare(){if(!q(BOX)?.checked)return;const batch=window.__batchAnalysis||[];for(const item of batch){if(item?.error||!item.data)continue;const d=Array.isArray(item.revisionClouds)?item.revisionClouds:await detectRevisionClouds(item.data);if(!d.length)continue;const cleaned=await removeRevisionClouds(item.data,d);item.data=cleaned.bytes;item.revisionCloudApplied=cleaned.count;}}
function hook(){inject();q('#batchAnalyze')?.addEventListener('click',()=>setTimeout(()=>analyze().catch(e=>console.error('[revision-clouds]',e)),120));const wrap=()=>{const current=window.__prepareBatchAnnotationOperations;if(typeof current!=='function')return setTimeout(wrap,20);if(current.__revisionCloudsWrapped)return;const wrapped=async()=>{await current();await prepare();};wrapped.__revisionCloudsWrapped=true;window.__prepareBatchAnnotationOperations=wrapped;};wrap();}
window.__prepareRevisionCloudOperations=prepare;window.__detectRevisionClouds=detectRevisionClouds;if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',hook);else hook();
