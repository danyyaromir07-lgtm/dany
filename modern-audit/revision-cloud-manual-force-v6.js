// Additive manual multi-raster exact-block fallback.
// Stable v5 always runs first unchanged. This route is reached only when v5 removed nothing,
// manual force is enabled, Preview/Apply is requested, and 2..8 raster candidates are all on one page.
// It selects only vector components that intersect those raster candidates and removes one unique safe BMC
// whose exact RGB, line width and stroke count equal the selected components. Unrelated same-style components
// elsewhere on the page are never included merely because they share color/width.
import {
  removeDetectedRevisionCloudsByExactFamily as baseRemove,
  isManualCloudForceEnabled as baseManualEnabled,
  clearManualCloudForcePreviewApprovals as baseClearApprovals,
} from './revision-cloud-manual-force-v5.js?v=20260818-curvedgray2';

const EPS=1e-5;
const approvals=new Set();
const MAIN='#batchRemoveRevisionClouds',FORCE='#batchForceRevisionClouds';
const same=(a,b)=>Math.abs(Number(a)-Number(b))<=EPS;
const sameRGB=(a,b)=>a&&b&&a.length>=3&&b.length>=3&&same(a[0],b[0])&&same(a[1],b[1])&&same(a[2],b[2]);
const area=r=>Math.max(0,Number(r?.[2]||0)-Number(r?.[0]||0))*Math.max(0,Number(r?.[3]||0)-Number(r?.[1]||0));
function diag(stage,extra={}){try{window.__cloudDiagnostic?.({stage,detail:'manual-cloud-force-v6',...extra});}catch(_){}}
function isRaster(c){return c?.source!=='vector-family'&&c?.source!=='vector-family-multi';}
function rasterCandidates(pages){const out=[];for(const p of pages||[]){const page=Number(p?.page||0);if(page<1)continue;for(const c of p?.clouds||[]){if(!isRaster(c)||!Array.isArray(c?.bbox)||c.bbox.length<4)continue;const bbox=c.bbox.slice(0,4).map(Number);if(bbox.every(Number.isFinite)&&area(bbox)>0)out.push({page,bbox,source:String(c?.source||'raster')});}}return out;}
function reset(){approvals.clear();window.__manualCloudMultiRasterExactApprovedFiles=[];}
function sync(){window.__manualCloudMultiRasterExactApprovedFiles=Array.from(approvals);}
function wire(){document.querySelector('#batchAnalyze')?.addEventListener('click',reset,true);document.querySelector('#batchClear')?.addEventListener('click',reset,true);document.addEventListener('change',e=>{if(e.target?.matches?.(MAIN)||e.target?.matches?.(FORCE))reset();});}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wire);else wire();
function fail(base,reason,extra={}){return{...base,details:[...(base?.details||[]),{removed:false,manualForce:true,mode:'manual-multiraster-exact-block',reason,...extra}]};}
function colorKey(cs,color){const name=String(cs||'');if(!/DeviceRGB|RGB/i.test(name)||!color||typeof color.length!=='number'||color.length<3)return null;return Array.from(color).slice(0,3).map(v=>Number(v).toPrecision(12)).join('|');}
function widthKey(w){return Number(w||0).toPrecision(12);}
function isRed(rgb){if(!rgb||rgb.length<3)return false;const [r,g,b]=Array.from(rgb).slice(0,3).map(Number);return r>=0.5&&r>=g+0.12&&r>=b+0.12;}
function rectGap(a,b){const dx=Math.max(0,Math.max(a[0],b[0])-Math.min(a[2],b[2]));const dy=Math.max(0,Math.max(a[1],b[1])-Math.min(a[3],b[3]));return Math.hypot(dx,dy);}
function rectIntersects(a,b){return a&&b&&a[0]<=b[2]&&a[2]>=b[0]&&a[1]<=b[3]&&a[3]>=b[1];}
function unionRect(a,b){if(!a)return b.slice();return[Math.min(a[0],b[0]),Math.min(a[1],b[1]),Math.max(a[2],b[2]),Math.max(a[3],b[3])];}
function familyUnion(strokes){let out=null;for(const s of strokes)out=unionRect(out,s.bbox);return out;}
function rectUnionArea(rects){if(!Array.isArray(rects)||!rects.length)return 0;const xs=[];for(const r of rects){if(r?.length>=4&&Number.isFinite(r[0])&&Number.isFinite(r[2]))xs.push(r[0],r[2]);}xs.sort((a,b)=>a-b);const ux=[];for(const x of xs)if(!ux.length||Math.abs(x-ux[ux.length-1])>EPS)ux.push(x);let total=0;for(let i=0;i+1<ux.length;i++){const x0=ux[i],x1=ux[i+1];if(x1<=x0)continue;const spans=[];for(const r of rects){if(!r||r.length<4||r[0]>=x1||r[2]<=x0)continue;const y0=Number(r[1]),y1=Number(r[3]);if(Number.isFinite(y0)&&Number.isFinite(y1)&&y1>y0)spans.push([y0,y1]);}if(!spans.length)continue;spans.sort((a,b)=>a[0]-b[0]||a[1]-b[1]);let y0=spans[0][0],y1=spans[0][1],covered=0;for(let j=1;j<spans.length;j++){const s=spans[j];if(s[0]<=y1+EPS){if(s[1]>y1)y1=s[1];}else{covered+=Math.max(0,y1-y0);y0=s[0];y1=s[1];}}covered+=Math.max(0,y1-y0);total+=(x1-x0)*covered;}return total;}
function collectFamilies(mupdf,page){const groups=new Map();const device=new mupdf.Device({strokePath(path,stroke,ctm,colorSpace,color,alpha){const ck=colorKey(colorSpace,color);if(!ck||!isRed(color))return;let bbox;try{bbox=Array.from(path.getBounds(stroke,ctm));}catch(_){return;}if(!bbox||bbox.length<4||!bbox.every(Number.isFinite))return;const w=Number(stroke?.getLineWidth?.()??stroke?.lineWidth??0);if(!Number.isFinite(w)||w<=0)return;const key=`${ck}::${widthKey(w)}`,rec={bbox,rgb:Array.from(color).slice(0,3).map(Number),lineWidth:w,alpha:Number(alpha??1)};if(!groups.has(key))groups.set(key,[]);groups.get(key).push(rec);}});page.runPageContents(device,mupdf.Matrix.identity);device.close?.();return groups;}
function connectedComponents(strokes,gapLimit){const n=strokes.length,seen=new Uint8Array(n),out=[];for(let i=0;i<n;i++){if(seen[i])continue;const stack=[i],comp=[];seen[i]=1;while(stack.length){const j=stack.pop();comp.push(strokes[j]);for(let k=0;k<n;k++){if(seen[k])continue;if(rectGap(strokes[j].bbox,strokes[k].bbox)<=gapLimit){seen[k]=1;stack.push(k);}}}out.push(comp);}return out.sort((a,b)=>b.length-a.length);}
function transformRect(rect,m){if(!rect||rect.length<4||!m||m.length<6)return rect?.slice?.()||rect;const pts=[[rect[0],rect[1]],[rect[2],rect[1]],[rect[0],rect[3]],[rect[2],rect[3]]].map(([x,y])=>[x*m[0]+y*m[2]+m[4],x*m[1]+y*m[3]+m[5]]);return[Math.min(...pts.map(p=>p[0])),Math.min(...pts.map(p=>p[1])),Math.max(...pts.map(p=>p[0])),Math.max(...pts.map(p=>p[1]))];}
function intersectionArea(a,b){return Math.max(0,Math.min(a[2],b[2])-Math.max(a[0],b[0]))*Math.max(0,Math.min(a[3],b[3])-Math.max(a[1],b[1]));}
function visibleScore(boxes,pageBounds){let inside=0,total=0;for(const b of boxes||[]){const a=area(b);if(a<=0)continue;total+=a;inside+=intersectionArea(b,pageBounds);}return total>0?inside/total:0;}
function alignBoxes(mupdf,page,boxes,pageBounds,rasterUnion){const raw=boxes.map(b=>b.slice()),rawScore=visibleScore(raw,pageBounds),rawHit=raw.some(b=>rectIntersects(b,rasterUnion));let converted=null,convertedScore=-1,convertedHit=false;try{const pageToPdf=Array.from(page.getTransform?.()||[]);if(pageToPdf.length>=6){const pdfToPage=mupdf.Matrix.invert(pageToPdf);converted=raw.map(b=>transformRect(b,pdfToPage));convertedScore=visibleScore(converted,pageBounds);convertedHit=converted.some(b=>rectIntersects(b,rasterUnion));}}catch(_){}const useConverted=!!converted&&(convertedScore>rawScore+0.25||(!rawHit&&convertedHit&&convertedScore>=0.98&&rawScore<0.98));return{boxes:useConverted?converted:raw,mode:useConverted?'pdf-user-to-page':'page'};}
function validateComponent(comp){if(comp.length<20)return null;const box=familyUnion(comp),w=Math.max(1,box[2]-box[0]),h=Math.max(1,box[3]-box[1]),aspect=Math.min(w,h)/Math.max(w,h);if(w<40||h<40||aspect<0.10)return null;const density=rectUnionArea(comp.map(s=>s.bbox))/Math.max(1,area(box)),limit=comp.length<=40?0.45:0.22;if(density>limit)return null;return{comp,box,strokes:comp.length,density};}
function candidateForFamily(mupdf,page,strokes,rasters,pageBounds){if(strokes.length<40||strokes.length>1200)return null;const lw=Math.abs(Number(strokes[0]?.lineWidth||0)),gapLimit=Math.max(8,Math.min(22,lw*30+8)),valid=connectedComponents(strokes,gapLimit).map(validateComponent).filter(Boolean);if(valid.length<rasters.length)return null;let rasterUnion=null;for(const r of rasters)rasterUnion=unionRect(rasterUnion,r.bbox);const aligned=alignBoxes(mupdf,page,valid.map(v=>v.box),pageBounds,rasterUnion);const chosen=[];for(let ri=0;ri<rasters.length;ri++){const hits=[];for(let ci=0;ci<valid.length;ci++)if(rectIntersects(aligned.boxes[ci],rasters[ri].bbox))hits.push(ci);if(hits.length!==1)return null;chosen.push(hits[0]);}if(new Set(chosen).size!==rasters.length)return null;for(const ci of new Set(chosen)){let hitCount=0;for(const r of rasters)if(rectIntersects(aligned.boxes[ci],r.bbox))hitCount++;if(hitCount!==1)return null;}const selected=Array.from(new Set(chosen)).map(i=>valid[i]);const selectedArea=selected.reduce((n,v)=>n+area(v.box),0),pageArea=Math.max(1,area(pageBounds));if(selectedArea/pageArea>0.08)return null;return{rgb:strokes[0].rgb,lineWidth:strokes[0].lineWidth,strokes:selected.reduce((n,v)=>n+v.strokes,0),clouds:rasters.length,componentSizes:selected.map(v=>v.strokes),componentDensity:selected.map(v=>v.density),coordinateMode:aligned.mode,componentBoxes:selected.map(v=>v.box)};}
function toText(buf){const bytes=buf?.asUint8Array?buf.asUint8Array():buf;let s='';for(let i=0;i<bytes.length;i+=0x8000)s+=String.fromCharCode(...bytes.subarray(i,Math.min(bytes.length,i+0x8000)));return s;}
function toBytes(s){const a=new Uint8Array(s.length);for(let i=0;i<s.length;i++)a[i]=s.charCodeAt(i)&255;return a;}
function markedStart(text,i){const from=Math.max(0,i-180),pre=text.slice(from,i),m=/(\/OC\s+\/[A-Za-z0-9_.-]+\s*$|\/?[A-Za-z0-9_.-]+\s*$)/.exec(pre);return m?from+m.index:i;}
function blocks(text){const re=/\b(?:BDC|BMC|EMC)\b/g,stack=[],out=[];let m;while((m=re.exec(text))){if(m[0]==='BDC'||m[0]==='BMC')stack.push({start:markedStart(text,m.index),op:m[0]});else if(stack.length){const o=stack.pop();out.push({start:o.start,end:re.lastIndex,op:o.op});}}return out;}
function refs(page){const c=page.getObject().get('Contents');if(!c)return[];if(c.isStream?.())return[c];const a=[];for(let i=0;i<Number(c.length||0);i++){const r=c.get(i);if(r?.isStream?.())a.push(r);}return a;}
const countS=t=>(t.match(/(?:^|[\r\n])\s*S\s*(?=[\r\n]|$)/g)||[]).length;
function exactRG(t,rgb){const re=/(^|[\s\r\n])([-+]?(?:\d+(?:\.\d*)?|\.\d+))\s+([-+]?(?:\d+(?:\.\d*)?|\.\d+))\s+([-+]?(?:\d+(?:\.\d*)?|\.\d+))\s+RG(?=\s|$)/gm;let m,seen=false;while((m=re.exec(t))){seen=true;if(!sameRGB([+m[2],+m[3],+m[4]],rgb))return false;}return seen;}
function exactW(t,width){const re=/(^|[\s\r\n])([-+]?(?:\d+(?:\.\d*)?|\.\d+))\s+w(?=\s|$)/gm;let m,seen=false;while((m=re.exec(t))){seen=true;if(!same(+m[2],width))return false;}return seen;}
function unsafe(t){return /\bBT\b|\bTj\b|\bTJ\b|\bBI\b|\bID\b|\bEI\b|\/[A-Za-z0-9_.-]+\s+Do\b|\bsh\b/.test(t)||/(?:^|[\r\n])\s*(?:f\*?|F|B\*?|b\*?)\s*(?=[\r\n]|$)/m.test(t);}
function exactHits(page,c){const out=[];for(const ref of refs(page)){let text;try{text=toText(ref.readStream());}catch(_){continue;}for(const b of blocks(text)){if(b.op!=='BMC')continue;const body=text.slice(b.start,b.end);if(countS(body)!==c.strokes||!exactRG(body,c.rgb)||!exactW(body,c.lineWidth)||unsafe(body))continue;out.push({ref,text,b});}}return out;}
async function removeMultiRasterExact(data,detectedPages,options={}){const file=String(options.file||''),context=String(options.context||''),rasters=rasterCandidates(detectedPages);if(rasters.length<2||rasters.length>8)return null;const pages=new Set(rasters.map(r=>r.page));if(pages.size!==1)return null;const pageNo=rasters[0].page;diag('cloud.multirasterexact.start',{file,page:pageNo,rasterCandidates:rasters.length});const mupdf=await import('https://cdn.jsdelivr.net/npm/mupdf@1.28.0/dist/mupdf.js');const doc=mupdf.PDFDocument.openDocument(new Uint8Array(data),'application/pdf');try{if(pageNo>doc.countPages())return null;const page=doc.loadPage(pageNo-1),pageBounds=Array.from(page.getBounds?.()||[0,0,0,0]),families=collectFamilies(mupdf,page),candidates=[];for(const strokes of families.values()){const c=candidateForFamily(mupdf,page,strokes,rasters,pageBounds);if(c)candidates.push(c);}if(candidates.length!==1){diag('cloud.multirasterexact.reject',{file,page:pageNo,reason:`familias raster-matched=${candidates.length}`});return null;}const c=candidates[0],hs=exactHits(page,c);if(hs.length!==1){diag('cloud.multirasterexact.reject',{file,page:pageNo,reason:`bloques BMC exactos=${hs.length}`,strokes:c.strokes});return null;}const h=hs[0];h.ref.writeStream(toBytes(h.text.slice(0,h.b.start)+h.text.slice(h.b.end)));const buf=doc.saveToBuffer('garbage=4,compress=yes,appearance=yes'),out=buf?.asUint8Array?new Uint8Array(buf.asUint8Array()):new Uint8Array(buf);diag('cloud.multirasterexact.accept',{file,page:pageNo,removedClouds:c.clouds,strokes:c.strokes,rgb:c.rgb,lineWidth:c.lineWidth,componentSizes:c.componentSizes,componentDensity:c.componentDensity,coordinateMode:c.coordinateMode});return{data:out,removed:c.clouds,manualForce:true,details:[{removed:true,page:pageNo,mode:'manual-multiraster-exact-block',removedClouds:c.clouds,strokes:c.strokes,rgb:c.rgb,lineWidth:c.lineWidth,componentSizes:c.componentSizes,componentDensity:c.componentDensity,coordinateMode:c.coordinateMode}]};}finally{doc.destroy();}}
export async function removeDetectedRevisionCloudsByExactFamily(data,detectedPages,options={}){const base=await baseRemove(data,detectedPages,options);if(Number(base?.removed||0)>0||!baseManualEnabled())return base;const context=String(options?.context||''),file=String(options?.file||''),rasters=rasterCandidates(detectedPages);if(rasters.length<2)return base;if(context!=='preview'&&context!=='apply')return base;if(context==='apply'&&(!file||!approvals.has(file)))return fail(base,'modo manual multicloud: primero abre «Previsualizar cambios» y verifica visualmente las nubes antes de Aplicar');try{const extra=await removeMultiRasterExact(data,detectedPages,{context,file});if(!extra||Number(extra.removed||0)<=0)return fail(base,'modo manual multicloud: no existe un único bloque exacto que corresponda 1:1 a las candidatas raster');if(context==='preview'&&file){approvals.add(file);sync();}return{...extra,details:[...(base?.details||[]),...(extra?.details||[])]};}catch(err){diag('cloud.multirasterexact.error',{file,error:err?.message||String(err)});return fail(base,`modo manual multicloud exacto: ${err?.message||String(err)}`);}}
export function isManualCloudForceEnabled(){return baseManualEnabled();}
export function clearManualCloudForcePreviewApprovals(){reset();baseClearApprovals();}
window.__revisionCloudManualForceV6={version:'6+multirasterexact1',get approvedFiles(){return Array.from(approvals);}};
