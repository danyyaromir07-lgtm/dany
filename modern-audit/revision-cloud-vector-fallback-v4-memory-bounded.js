// Memory-bounded replacement for revision-cloud-vector-fallback-v1.
// Detection thresholds are intentionally preserved; only collection is bounded.
const CHECKBOX='#batchRemoveRevisionClouds',STATUS='#batchStatus',SUMMARY='#batchSummary';
const EPS=1e-6,MAX_KEEP=1201,sleep=ms=>new Promise(r=>setTimeout(r,ms));
let mupdfPromise=null;const VERSION='memory-bounded-v4';
const now=()=>globalThis.performance?.now?.()??Date.now(),q=s=>document.querySelector(s);
function diag(event){try{window.__cloudDiagnostic?.(event)}catch(_){}}
function isRed(rgb){if(!rgb||rgb.length<3)return false;const[r,g,b]=Array.from(rgb).slice(0,3).map(Number);return r>=.50&&r>=g+.12&&r>=b+.12}
function colorKey(cs,color){const name=String(cs||'');if(!/DeviceRGB|RGB/i.test(name)||!color||typeof color.length!=='number'||color.length<3)return null;return Array.from(color).slice(0,3).map(v=>Number(v).toPrecision(12)).join('|')}
const widthKey=w=>Number(w||0).toPrecision(12);
function unionRect(a,b){return a?[Math.min(a[0],b[0]),Math.min(a[1],b[1]),Math.max(a[2],b[2]),Math.max(a[3],b[3])]:b.slice()}
function rectGap(a,b){const dx=Math.max(0,Math.max(a[0],b[0])-Math.min(a[2],b[2])),dy=Math.max(0,Math.max(a[1],b[1])-Math.min(a[3],b[3]));return Math.hypot(dx,dy)}
const area=r=>Math.max(0,r[2]-r[0])*Math.max(0,r[3]-r[1]);
function rectUnionArea(rects){if(!rects?.length)return 0;const xs=[];for(const r of rects)if(r?.length>=4&&Number.isFinite(r[0])&&Number.isFinite(r[2]))xs.push(r[0],r[2]);xs.sort((a,b)=>a-b);const u=[];for(const x of xs)if(!u.length||Math.abs(x-u.at(-1))>EPS)u.push(x);let total=0;for(let i=0;i+1<u.length;i++){const x0=u[i],x1=u[i+1];if(x1<=x0)continue;const spans=[];for(const r of rects){if(!r||r[0]>=x1||r[2]<=x0)continue;const y0=Number(r[1]),y1=Number(r[3]);if(Number.isFinite(y0)&&Number.isFinite(y1)&&y1>y0)spans.push([y0,y1])}if(!spans.length)continue;spans.sort((a,b)=>a[0]-b[0]||a[1]-b[1]);let y0=spans[0][0],y1=spans[0][1],covered=0;for(let j=1;j<spans.length;j++){const s=spans[j];if(s[0]<=y1+EPS)y1=Math.max(y1,s[1]);else{covered+=Math.max(0,y1-y0);y0=s[0];y1=s[1]}}covered+=Math.max(0,y1-y0);total+=(x1-x0)*covered}return total}
async function loadMuPDF(){if(!mupdfPromise)mupdfPromise=import('https://cdn.jsdelivr.net/npm/mupdf@1.28.0/dist/mupdf.js');return mupdfPromise}
function collectFamilies(mupdf,page){const groups=new Map();let device=null;try{device=new mupdf.Device({strokePath(path,stroke,ctm,colorSpace,color,alpha){const ck=colorKey(colorSpace,color);if(!ck||!isRed(color))return;let bbox;try{bbox=Array.from(path.getBounds(stroke,ctm))}catch(_){return}if(!bbox||bbox.length<4)return;const w=Number(stroke?.getLineWidth?.()??stroke?.lineWidth??0),key=`${ck}::${widthKey(w)}`;let g=groups.get(key);if(!g){g={count:0,strokes:[],rgb:[Number(color[0]),Number(color[1]),Number(color[2])],lineWidth:w};groups.set(key,g)}g.count++;if(g.strokes.length<MAX_KEEP)g.strokes.push({bbox,rgb:g.rgb,lineWidth:w,alpha:Number(alpha??1)})}});page.runPageContents(device,mupdf.Matrix.identity);return groups}finally{try{device?.close?.()}catch(_){}}}
function connectedComponents(strokes,gapLimit){const n=strokes.length;if(!n)return[];const parent=Int32Array.from({length:n},(_,i)=>i),rank=new Uint8Array(n),order=Array.from({length:n},(_,i)=>i).sort((a,b)=>strokes[a].bbox[0]-strokes[b].bbox[0]);const find=x=>{let r=x;while(parent[r]!==r)r=parent[r];while(parent[x]!==x){const p=parent[x];parent[x]=r;x=p}return r};const union=(a,b)=>{let ra=find(a),rb=find(b);if(ra===rb)return;if(rank[ra]<rank[rb]){const t=ra;ra=rb;rb=t}parent[rb]=ra;if(rank[ra]===rank[rb])rank[ra]++};let active=[];for(const i of order){const r=strokes[i].bbox,minX=r[0]-gapLimit,next=[];for(const j of active){const b=strokes[j].bbox;if(b[2]<minX)continue;next.push(j);const dy=Math.max(0,Math.max(r[1],b[1])-Math.min(r[3],b[3]));if(dy<=gapLimit&&rectGap(r,b)<=gapLimit)union(i,j)}next.push(i);active=next}const map=new Map();for(let i=0;i<n;i++){const r=find(i);if(!map.has(r))map.set(r,[]);map.get(r).push(strokes[i])}return[...map.values()].sort((a,b)=>b.length-a.length)}
function evaluateFamily(key,g,pageBounds){const count=Number(g?.count||0),strokes=g?.strokes||[],base={key,strokes:count,rgb:g?.rgb,lineWidth:g?.lineWidth};if(count<20)return{candidate:null,reason:'menos de 20 trazos',metrics:base};if(count>1200)return{candidate:null,reason:'más de 1200 trazos',metrics:base};const lw=Math.abs(Number(g.lineWidth||0)),gapLimit=Math.max(8,Math.min(22,lw*30+8)),comps=connectedComponents(strokes,gapLimit);if(!comps.length)return{candidate:null,reason:'sin componentes conectados',metrics:{...base,gapLimit}};const main=comps[0],mainRatio=main.length/count;if(main.length<20)return{candidate:null,reason:'componente principal menor de 20 trazos',metrics:{...base,gapLimit,components:comps.length,main:main.length,mainRatio}};if(mainRatio<.90)return{candidate:null,reason:`familia dispersa ${main.length}/${count}`,metrics:{...base,gapLimit,components:comps.length,main:main.length,mainRatio}};let union=null,sumBoxArea=0;for(const s of main){union=unionRect(union,s.bbox);sumBoxArea+=area(s.bbox)}const w=Math.max(1,union[2]-union[0]),h=Math.max(1,union[3]-union[1]);if(w<40||h<40)return{candidate:null,reason:'bbox menor de 40×40',metrics:{...base,gapLimit,components:comps.length,main:main.length,mainRatio,bbox:union,width:w,height:h}};const pageArea=Math.max(1,area(pageBounds)),frac=area(union)/pageArea;if(frac<.00015)return{candidate:null,reason:'familia demasiado pequeña respecto a la página',metrics:{...base,bbox:union,fraction:frac}};if(frac>.08)return{candidate:null,reason:'familia ocupa demasiado de la página',metrics:{...base,bbox:union,fraction:frac}};const bboxArea=Math.max(1,area(union)),rawDensity=sumBoxArea/bboxArea,occupiedArea=rectUnionArea(main.map(s=>s.bbox)),sparse=occupiedArea/bboxArea;if(sparse>.25)return{candidate:null,reason:'densidad de unión demasiado alta',metrics:{...base,bbox:union,fraction:frac,density:sparse,rawDensity}};const aspect=Math.min(w,h)/Math.max(w,h);if(aspect<.10)return{candidate:null,reason:'geometría demasiado lineal/alargada',metrics:{...base,bbox:union,fraction:frac,density:sparse,aspect}};const outside=count-main.length,outsideLimit=Math.max(2,Math.floor(count*.05));if(outside>outsideLimit)return{candidate:null,reason:`demasiados trazos fuera del componente principal (${outside})`,metrics:{...base,bbox:union,outside,outsideLimit}};return{candidate:{key,bbox:union,rgb:main[0].rgb,lineWidth:main[0].lineWidth,strokeCount:main.length,sparse,fraction:frac,source:'vector-family'},reason:null,metrics:{...base,gapLimit,components:comps.length,main:main.length,mainRatio,bbox:union,width:w,height:h,fraction:frac,density:sparse,rawDensity,occupiedArea,aspect,outside,outsideLimit}}}
export async function detectVectorCloudFallback(data,context={}){const t0=now(),mupdf=await loadMuPDF(),doc=mupdf.PDFDocument.openDocument(new Uint8Array(data),'application/pdf'),out=[],debugPages=[];try{for(let i=0;i<doc.countPages();i++){let page=null;try{const pt=now();page=doc.loadPage(i);const groups=collectFamilies(mupdf,page),pageBounds=Array.from(page.getBounds()),candidates=[],families=[];for(const[key,g]of groups){const ev=evaluateFamily(key,g,pageBounds);families.push({accepted:!!ev.candidate,reason:ev.reason,...ev.metrics});if(ev.candidate)candidates.push(ev.candidate)}debugPages.push({page:i+1,groups:groups.size,candidates:candidates.length,families,ms:Math.round((now()-pt)*10)/10});if(context.file){for(const f of families)diag({stage:f.accepted?'cloud.fallback.real.accept':'cloud.fallback.real.reject',detail:f.accepted?'familia aceptada por detector real':'familia rechazada por detector real',file:context.file,page:i+1,strokes:f.strokes,rgb:f.rgb,lineWidth:f.lineWidth,bbox:f.bbox,reason:f.reason});diag({stage:'cloud.perf.vector.page',detail:'vector-fallback-memory-bounded-v4',file:context.file,page:i+1,reason:`familias=${groups.size}`})}if(candidates.length===1){const c=candidates[0];out.push({page:i+1,clouds:[{bbox:c.bbox,source:'vector-family',exactRGB:c.rgb,exactLineWidth:c.lineWidth,vectorFamilyKey:c.key,vectorStrokeCount:c.strokeCount}]})}}finally{try{page?.destroy?.()}catch(_){}}}}finally{doc.destroy()}out.debugPages=debugPages;if(context.file)diag({stage:'cloud.perf.vector.file',detail:'vector-fallback-memory-bounded-v4',file:context.file,reason:`${Math.round((now()-t0)*10)/10} ms`});return out}
function refreshCloudReport(batch){let total=0;for(const item of batch)total+=Number(item?.revisionCloudCount||0);const status=q(STATUS);if(status&&total)status.textContent=`☁️ ${total} nube${total===1?'':'s'} de revisión detectada${total===1?'':'s'}.`;const summary=q(SUMMARY);if(summary){const clean=(summary.textContent||'').replace(/ · ☁️[^·]*/g,'').trim();summary.textContent=`${clean} · ☁️ ${total} nube${total===1?'':'s'} detectada${total===1?'':'s'}`;summary.classList.remove('hidden')}}
async function runFallbackAfterRaster(rasterBaseline){const started=now(),selected=q(CHECKBOX)?.checked===true;let batch=[],added=0,debug=[],status='not-selected',error=null;try{if(!selected)return;status='waiting-raster';for(let i=0;i<300;i++){batch=Array.isArray(window.__batchAnalysis)?window.__batchAnalysis:[];const baseReady=batch.length&&batch.every(x=>x?.error||x?.data),rasterDone=window.__revisionCloudDebug!==rasterBaseline;if(baseReady&&rasterDone){status='running';break}await sleep(100)}if(status!=='running'){status=batch.length?'raster-timeout':'no-batch';return}for(const item of batch){if(item?.error||!item?.data||Number(item.revisionCloudCount||0)>0)continue;const t=now();try{const found=await detectVectorCloudFallback(item.data,{file:item.name});debug.push({name:item.name,pages:found.debugPages||[],ms:Math.round((now()-t)*10)/10});if(found.length){item.revisionClouds=found;item.revisionCloudCount=found.reduce((n,p)=>n+(p.clouds?.length||0),0);item.revisionCloudVectorFallback=true;added+=item.revisionCloudCount}}catch(err){item.revisionCloudVectorError=err?.message||String(err);diag({stage:'cloud.vector.error',detail:'vector-fallback-memory-bounded-v4',file:item.name,error:item.revisionCloudVectorError})}await sleep(0)}status='done';if(added)refreshCloudReport(batch)}catch(err){error=err?.message||String(err);status='error';throw err}finally{window.__revisionCloudVectorFallbackDebug={added,version:VERSION,debug,batch:batch.map(x=>({name:x?.name,count:x?.revisionCloudCount||0,vector:!!x?.revisionCloudVectorFallback,error:x?.revisionCloudVectorError||null})),status,elapsedMs:Math.round((now()-started)*10)/10,error};diag({stage:'cloud.fallback.done',detail:'vector-fallback-memory-bounded-v4',reason:`estado=${status} · añadidas=${added}`})}}
function wire(){q('#batchAnalyze')?.addEventListener('click',()=>{const rasterBaseline=window.__revisionCloudDebug;diag({stage:'cloud.fallback.start',detail:'vector-fallback-memory-bounded-v4',reason:'esperando final raster del ciclo actual'});runFallbackAfterRaster(rasterBaseline).catch(e=>console.error('[cloud-vector-fallback-v4]',e))})}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wire);else wire();
window.__revisionCloudVectorFallbackV4={version:VERSION};
