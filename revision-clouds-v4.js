let detectorPromise = null;
const CHECKBOX = '#batchRemoveRevisionClouds';
const STATUS = '#batchStatus';
const SUMMARY = '#batchSummary';
const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
const q = (s) => document.querySelector(s);
function ensureCheckbox() { if (q(CHECKBOX)) return q(CHECKBOX); const host=q('#batchRemoveComments')?.closest('.option-box'); if(!host)return null; const box=document.createElement('div'); box.className='option-box'; box.style.marginTop='10px'; box.innerHTML='<label><input id="batchRemoveRevisionClouds" type="checkbox"><span>☁️ Eliminar nubes de revisión gráficas</span></label><small>Detector opcional. Solo se ejecuta cuando lo activas.</small>'; host.parentElement?.insertBefore(box,host.nextElementSibling); return box.querySelector(CHECKBOX); }
async function loadMuPDF(){if(!detectorPromise)detectorPromise=import('https://cdn.jsdelivr.net/npm/mupdf@1.28.0/dist/mupdf.js');return detectorPromise;}
function isRed(r,g,b){return r>=135&&r>=g+35&&r>=b+35&&g<=190&&b<=190;}
function dilate(mask,w,h){const out=new Uint8Array(mask.length);for(let y=0;y<h;y++){for(let x=0;x<w;x++){let hit=0;for(let dy=-1;dy<=1&&!hit;dy++){for(let dx=-1;dx<=1;dx++){const xx=x+dx,yy=y+dy;if(xx>=0&&xx<w&&yy>=0&&yy<h&&mask[yy*w+xx]){hit=1;break}}}out[y*w+x]=hit;}}return out;}
function components(mask,w,h){const seen=new Uint8Array(w*h),stack=new Int32Array(w*h),out=[];for(let sy=0;sy<h;sy++)for(let sx=0;sx<w;sx++){const seed=sy*w+sx;if(!mask[seed]||seen[seed])continue;let top=0,area=0,minX=sx,maxX=sx,minY=sy,maxY=sy;stack[top++]=seed;seen[seed]=1;while(top){const idx=stack[--top],y=(idx/w)|0,x=idx-y*w;area++;if(x<minX)minX=x;if(x>maxX)maxX=x;if(y<minY)minY=y;if(y>maxY)maxY=y;const n=[idx-1,idx+1,idx-w,idx+w];if(x>0&&mask[n[0]]&&!seen[n[0]]){seen[n[0]]=1;stack[top++]=n[0]}if(x+1<w&&mask[n[1]]&&!seen[n[1]]){seen[n[1]]=1;stack[top++]=n[1]}if(y>0&&mask[n[2]]&&!seen[n[2]]){seen[n[2]]=1;stack[top++]=n[2]}if(y+1<h&&mask[n[3]]&&!seen[n[3]]){seen[n[3]]=1;stack[top++]=n[3]}}const bw=maxX-minX+1,bh=maxY-minY+1,density=area/Math.max(1,bw*bh),fraction=bw*bh/Math.max(1,w*h);if(area<250||bw<50||bh<50||density>0.20||fraction>0.70)continue;let rough=0,rows=0,prev=null;for(let y=minY;y<=maxY;y++){let lo=w,hi=-1;for(let x=minX;x<=maxX;x++)if(mask[y*w+x]){lo=Math.min(lo,x);hi=Math.max(hi,x)}if(hi>=0){rows++;if(prev)rough+=Math.abs(lo-prev[0])+Math.abs(hi-prev[1]);prev=[lo,hi]}}const roughness=rough/Math.max(1,rows);if(roughness<0.03)continue;const crop=new Uint8Array(bw*bh);for(let y=0;y<bh;y++)for(let x=0;x<bw;x++)crop[y*bw+x]=mask[(minY+y)*w+(minX+x)];out.push({minX,minY,bw,bh,density,roughness,area,crop,cropW:bw,cropH:bh});}return out.sort((a,b)=>b.area-a.area).slice(0,4);}
async function detectPage(mupdf,page,scale=0.18){const pix=page.toPixmap(mupdf.Matrix.scale(scale,scale),mupdf.ColorSpace.DeviceRGB,false,false),w=pix.getWidth(),h=pix.getHeight(),c=pix.getNumberOfComponents();if(!w||!h||c<3)return[];const px=pix.getPixels(),red=new Uint8Array(w*h);for(let i=0,p=0;i<red.length;i++,p+=c)red[i]=isRed(px[p],px[p+1],px[p+2])?1:0;const mask=dilate(red,w,h),found=components(mask,w,h),bounds=page.getBounds();return found.map(x=>({...x,scale,bbox:[bounds[0]+x.minX/scale,bounds[1]+x.minY/scale,bounds[0]+(x.minX+x.bw)/scale,bounds[1]+(x.minY+x.bh)/scale]}));}
export async function detectRevisionClouds(data){const mupdf=await loadMuPDF(),doc=mupdf.PDFDocument.openDocument(new Uint8Array(data),'application/pdf'),out=[];try{for(let i=0;i<doc.countPages();i++){const page=doc.loadPage(i);let found=await detectPage(mupdf,page,0.18);if(!found.length)found=await detectPage(mupdf,page,0.30);if(found.length)out.push({page:i+1,clouds:found});}}finally{doc.destroy()}return out;}
async function waitForAnalysis(){for(let i=0;i<600;i++){const a=window.__batchAnalysis;if(Array.isArray(a)&&a.length&&a.every(x=>x?.error||x?.data))return a;await sleep(100)}return Array.isArray(window.__batchAnalysis)?window.__batchAnalysis:[];}
function report(total,errors,debug=''){const status=q(STATUS);if(status)status.textContent=total?`☁️ ${total} nube${total===1?'':'s'} de revisión detectada${total===1?'':'s'}.`:`☁️ Detector de nubes: 0 detectadas${errors?` · errores: ${errors}`:''}${debug?` · ${debug}`:''}.`;const summary=q(SUMMARY);if(summary){const clean=(summary.textContent||'').replace(/ · ☁️[^·]*/g,'').trim();summary.textContent=`${clean} · ☁️ ${total} nube${total===1?'':'s'} detectada${total===1?'':'s'}`;summary.classList.remove('hidden');}}
async function analyzeClouds(){const box=ensureCheckbox();if(!box?.checked)return;const batch=await waitForAnalysis();if(!batch.length){report(0,1,'sin datos de análisis');return;}let total=0,errors=0,filesChecked=0;for(const item of batch){if(item?.error||!item?.data)continue;filesChecked++;try{const found=await detectRevisionClouds(item.data);item.revisionClouds=found;item.revisionCloudCount=found.reduce((n,p)=>n+p.clouds.length,0);total+=item.revisionCloudCount;}catch(err){item.revisionClouds=[];item.revisionCloudCount=0;item.revisionCloudError=err?.message||String(err);errors++;}}window.__revisionCloudDebug={total,errors,filesChecked,batch:batch.map(x=>({name:x?.name,count:x?.revisionCloudCount||0,error:x?.revisionCloudError||null}))};const detail=errors?`errores ${errors}`:`archivos ${filesChecked}`;report(total,errors,detail);}
function dataUrlToBytes(url){const b64=url.split(',')[1]||'',bin=atob(b64),out=new Uint8Array(bin.length);for(let i=0;i<out.length;i++)out[i]=bin.charCodeAt(i);return out;}
function buildOverlay(mupdf,page,cloud){const scale=0.30,pix=page.toPixmap(mupdf.Matrix.scale(scale,scale),mupdf.ColorSpace.DeviceRGB,false,false),w=pix.getWidth(),h=pix.getHeight(),c=pix.getNumberOfComponents(),px=pix.getPixels(),x0=Math.max(0,Math.floor(cloud.bbox[0]*scale)-4),y0=Math.max(0,Math.floor(cloud.bbox[1]*scale)-4),x1=Math.min(w,Math.ceil(cloud.bbox[2]*scale)+4),y1=Math.min(h,Math.ceil(cloud.bbox[3]*scale)+4),cw=Math.max(1,x1-x0),ch=Math.max(1,y1-y0),canvas=document.createElement('canvas');canvas.width=cw;canvas.height=ch;const ctx=canvas.getContext('2d'),img=ctx.createImageData(cw,ch),dw=cloud.cropW,dh=cloud.cropH;for(let y=0;y<ch;y++)for(let x=0;x<cw;x++){const gx=x0+x,gy=y0+y,pp=(gy*w+gx)*c,r=px[pp],g=px[pp+1],b=px[pp+2];if(!isRed(r,g,b))continue;const ix=Math.max(0,Math.min(dw-1,Math.floor((gx/scale-cloud.bbox[0])*cloud.scale))),iy=Math.max(0,Math.min(dh-1,Math.floor((gy/scale-cloud.bbox[1])*cloud.scale)));if(cloud.crop[iy*dw+ix]){const o=(y*cw+x)*4;img.data[o]=255;img.data[o+1]=255;img.data[o+2]=255;img.data[o+3]=255;}}ctx.putImageData(img,0,0);return{png:dataUrlToBytes(canvas.toDataURL('image/png')),x:x0/scale,yTop:y0/scale,w:cw/scale,h:ch/scale};}
async function applyClouds(){const box=ensureCheckbox();if(!box?.checked)return;const batch=Array.isArray(window.__batchAnalysis)?window.__batchAnalysis:[];if(!batch.length)return;const mupdf=await loadMuPDF();const {PDFDocument}=await import('https://esm.sh/pdf-lib@1.17.1');for(const item of batch){if(item?.error||!item.data||!Array.isArray(item.revisionClouds)||!item.revisionClouds.length)continue;const pdf=await PDFDocument.load(item.data,{updateMetadata:false,ignoreEncryption:false}),m=mupdf.PDFDocument.openDocument(new Uint8Array(item.data),'application/pdf');try{for(const p of item.revisionClouds){const page=m.loadPage(p.page-1),target=pdf.getPages()[p.page-1];for(const cloud of p.clouds){const o=buildOverlay(mupdf,page,cloud),img=await pdf.embedPng(o.png);target.drawImage(img,{x:o.x,y:target.getHeight()-o.yTop-o.h,width:o.w,height:o.h,opacity:1});}}}finally{m.destroy()}item.data=await pdf.save({useObjectStreams:true,addDefaultPage:false});item.revisionCloudApplied=item.revisionCloudCount||0;}}
function wrapApply(){const base=window.__prepareBatchAnnotationOperations;if(typeof base!=='function'||base.__cloudSafeWrap)return false;const wrapped=async function(){await base();await applyClouds();};wrapped.__cloudSafeWrap=true;window.__prepareBatchAnnotationOperations=wrapped;return true;}
function installBatchAssignmentHook(){if(window.__revisionCloudAssignmentHook)return;let current=window.__batchAnalysis;try{Object.defineProperty(window,'__batchAnalysis',{configurable:true,enumerable:true,get(){return current;},set(value){current=value;if(q(CHECKBOX)?.checked&&Array.isArray(value)&&value.length){setTimeout(()=>analyzeClouds().catch(err=>{window.__revisionCloudDebug={total:0,errors:1,filesChecked:0,hookError:err?.message||String(err)};report(0,1,err?.message||String(err));}),0);}}});window.__revisionCloudAssignmentHook=true;}catch(_){window.__revisionCloudAssignmentHook=false;}}
function wire(){ensureCheckbox();installBatchAssignmentHook();document.addEventListener('change',event=>{if(event.target?.matches?.(CHECKBOX)&&event.target.checked)loadMuPDF().catch(err=>{window.__revisionCloudDebug={total:0,errors:1,error:err?.message||String(err)};report(0,1,err?.message||String(err));});});const retry=()=>{if(!wrapApply())setTimeout(retry,50)};retry();}
window.__detectRevisionClouds=detectRevisionClouds;window.__analyzeRevisionClouds=analyzeClouds;if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wire);else wire();
