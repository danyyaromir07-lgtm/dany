// Compatibility banner for the global analysis-completion coordinator.
// It no longer decides completion on its own, preventing a green state before cloud/adaptive checks finish.
const ANALYZE='#batchAnalyze',BANNER_ID='batchCheckCompletion';
function ensureBanner(){let b=document.getElementById(BANNER_ID);if(b)return b;b=document.createElement('div');b.id=BANNER_ID;b.setAttribute('role','status');b.setAttribute('aria-live','polite');Object.assign(b.style,{display:'none',marginTop:'10px',padding:'12px 14px',borderRadius:'10px',border:'1px solid rgba(80,80,80,.22)',fontWeight:'700',lineHeight:'1.35'});const s=document.querySelector('#batchStatus');if(s?.parentNode)s.insertAdjacentElement('afterend',b);return b;}
function setBanner(mode,title,detail){const b=ensureBanner();if(!b)return;b.style.display='block';if(mode==='done'){b.style.background='rgba(34,197,94,.11)';b.style.borderColor='rgba(34,197,94,.38)';}else if(mode==='warning'){b.style.background='rgba(245,158,11,.11)';b.style.borderColor='rgba(245,158,11,.42)';}else{b.style.background='rgba(245,158,11,.11)';b.style.borderColor='rgba(245,158,11,.42)';}b.innerHTML=`<div>${title}</div><div style="font-weight:500;font-size:.9rem;margin-top:3px;opacity:.82">${detail}</div>`;}
function renderState(state=window.__analysisCompletionState){if(!state)return;if(state.running){setBanner('busy','⏳ COMPROBACIÓN EN CURSO',state.stage?`Etapa actual: ${state.stage}. Espera a la confirmación final antes de dar los resultados por definitivos.`:'Analizando. Espera a la confirmación final antes de dar los resultados por definitivos.');return;}if(state.complete){setBanner('done','✅ COMPROBACIÓN COMPLETA','Todas las comprobaciones seleccionadas de esta ejecución han terminado. Ya puedes confiar en los resultados y abrir Preview o Aplicar.');return;}if(state.timeout){setBanner('warning','⚠️ COMPROBACIÓN INCOMPLETA','Un operador no publicó su finalización. Aplicar permanece bloqueado para evitar trabajar con resultados parciales.');return;}setBanner('warning','⚠️ COMPROBACIÓN FINALIZADA CON AVISOS','Todos los operadores seleccionados terminaron, pero hay avisos que conviene revisar en los diagnósticos.');}
function wire(){ensureBanner();document.querySelector(ANALYZE)?.addEventListener('click',()=>setBanner('busy','⏳ COMPROBACIÓN EN CURSO','No des por definitivos los resultados hasta que aparezca la confirmación final.'),true);window.addEventListener('analysis-completion-state',e=>renderState(e.detail));renderState();}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wire);else wire();
